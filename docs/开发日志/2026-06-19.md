# 2026-06-19

## 配置与服务启动

### 今日目标

运行 pytest 和 uvicorn。

### 完成内容

- 完成配置系统切片：确认 `backend/app/config.py` 已通过 `pydantic-settings` 集中读取配置。
- 新增 `backend/tests/test_config.py`，覆盖默认配置、环境变量覆盖、`get_settings()` 缓存行为。
- 确认当前实际虚拟环境位于项目根目录 `.venv/`，不是 `backend/.venv/`。
- 使用根目录虚拟环境运行后端测试。

### 执行命令

```bash
pytest

uvicorn app.main:app --reload

.venv\Scripts\python.exe -m pytest backend
```

### 验证结果

- pytest 正常显示结果。
- uvicorn 正常运行。
- 配置系统测试通过：`5 passed, 11 warnings in 1.97s`。
- 5 个测试包括：`backend/tests/test_config.py` 3 个，`backend/tests/test_health.py` 2 个。

### 遇到的问题

- pytest 显示 `pyproject.toml` 中有 `asyncio_default_fixture_loop_scope` 配置项未配置，原因是在未来版本中，该配置项将默认为 `function` 值，此处建议显式指明，以免出现不可预知的问题。
- uvicorn 显示未能成功获取网站图标。
- Cline 工具命令默认运行在新的 Windows CMD 会话中，不会执行 Git Bash 的 `.bashrc` 自动激活逻辑，因此直接运行 `pytest` 时找不到命令。
- 当前 Python 3.14 下，FastAPI / Starlette / pytest-asyncio 产生了若干 `DeprecationWarning`，提示部分 asyncio API 将在 Python 3.16 移除。

### 解决方式

- 在 `pyproject.toml` 中将对应配置项设为 `function` 以解决问题。
- 当前阶段网站无图标，属于正常情况。
- 通过 `where python` 和 `where pytest` 确认真实虚拟环境位于项目根目录 `.venv/`。
- 后续在工具命令中使用 `.venv\Scripts\python.exe -m pytest backend`，避免依赖终端是否已激活虚拟环境。
- warning 当前不影响测试结果，后续可考虑使用 Python 3.11 / 3.12 作为项目开发版本，减少依赖兼容噪音。

### 学到的东西

- `pydantic-settings` 可以把 `.env` / 环境变量映射为 Python 配置对象。
- `get_settings()` 使用 `lru_cache` 后，多次调用会返回同一个配置实例，避免重复读取配置。
- Git Bash 自动激活虚拟环境依赖 shell 函数；新开的 CMD 会话不会继承这个状态。
- 用 `python -m pytest` 比直接执行 `pytest` 更稳定，因为它明确指定了使用哪个 Python 环境。

### 仍然不理解的问题

- Python 3.14 是否适合作为本项目长期开发版本，后续需要结合依赖兼容性再判断。

### 下一步

- 进入下一个后端切片：基础数据模型或 API 路由骨架。

---

## 学习流程整理

### 今日目标

- 将“你手动配置，我指导解释”的学习协作方式固化到文档中。
- 整理已有学习流程文档，方便后续切换对话后继续遵循同一套流程。

### 完成内容

- 新增 `docs/学习协作流程.md`，作为后续学习协作的统一入口。
- 更新环境配置学习指南，明确后续默认由开发者手动执行配置、安装、启动和测试命令。
- 更新 `docs/决策记录/0001-采用混合式环境配置学习法.md`，将方案 C 调整为“手动配置优先的混合式环境配置学习法”。
- 明确当前虚拟环境实际位于项目根目录 `.venv/`。

### 执行命令

```bash
# 本轮为文档整理，未运行测试或服务命令。
```

### 验证结果

- 文档已形成统一入口：`docs/学习协作流程.md`。
- 后续切换对话时，可以优先读取该文件恢复协作方式。

### 遇到的问题

- 原有流程信息分散在多个文档中。
- 环境配置指南中部分内容仍偏向 AI 代配置，不符合后续手动配置学习目标。

### 解决方式

- 新增统一入口文档，集中说明角色分工、切片流程、手动验证规则和对话恢复方式。
- 修正环境配置指南和决策记录中的分工表述。

### 学到的东西

- 学习型项目需要把“协作方式”也文档化，否则切换对话后容易丢失节奏。
- 环境配置命令由自己手动执行，更有利于记忆命令、理解路径和定位错误。

### 仍然不理解的问题

- 暂无。

### 下一步

- 按 `docs/学习协作流程.md` 进入数据库基础切片。
- 先讲解 SQLAlchemy async、`Base`、`engine`、`session`，再设计最小 `Problem` 模型。