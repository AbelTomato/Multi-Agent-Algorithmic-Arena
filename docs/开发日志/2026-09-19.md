# 2026-09-19

### 00:07 Task 6 收口：ECS 脱敏运行参数审计部署

## 完成内容

- 经明确批准，在 Alibaba Cloud Linux 3 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）安装 controller 脱敏 JSONL 审计功能、systemd 审计状态目录和三天 logrotate 配置。
- controller 审计文件为 `/var/lib/multi-agent-arena-sandbox-audit/tasks.jsonl`；目录权限为 `750 arena-sandbox:arena-sandbox`，文件权限为 `640 arena-sandbox:arena-sandbox`。
- 安装最终使用 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` 构建的静态 Linux/amd64 二进制，SHA-256 为 `676db374aef3fa3282d8b80e4adebe6b29ec6eba5a4b66ec96f6a44fd93fecd2`。
- systemd unit 设置 `ARENA_SANDBOX_AUDIT_LOG`、`StateDirectory=multi-agent-arena-sandbox-audit` 和 `StateDirectoryMode=0750`；logrotate 配置每日轮转、`rotate 3`、`maxage 3`、压缩和受限创建权限。
- 回滚材料保留在权限 `700` 的 `/opt/multi-agent-arena/backend-release.h0l5La/sandbox-audit-20260918-2342`，包含原 controller 和 systemd unit；部署未修改数据库、backend 配置、Nginx、Provider 或评测开关。

## 执行与验证结果

- 本地静态二进制和部署工件均通过 SHA-256 校验；sandbox 全量 Go 测试、`go vet ./...`、部署静态测试 `9 passed` 和 `git diff --check` 均通过。
- ECS controller 为 `enabled/active`，私网 `172.30.0.1:8001/health` 返回 `200`，监听边界保持为 `172.30.0.1:8001`。
- controller network gate 在最终启动时验证成功：`app_arena_sandbox` 为 internal `bridge`、网段 `172.30.0.0/24`、网关 `172.30.0.1`。
- `logrotate -d` 验证通过；backend `/health=200`、`/api/problems=200`；合法 `problem_id=2` 关闭路径返回 `503`；Arena owner 标签容器数量为 `0`。
- 未触发 Provider 调用、真实评测或候选容器创建/删除，因此审计 JSONL 当前为空是预期状态。

## 遇到的问题与解决方式

- 首次部署构建的 controller 为动态链接，ECS 运行时缺少 `GLIBC_2.34`，新进程启动失败；自动回滚恢复旧 controller。随后确认 ECS 网络门禁、旧 controller 和关闭路径正常。
- 初始回滚试图覆盖运行中的 ELF，触发 `Text file busy`；后续调整为先停止 controller 再替换或恢复文件。
- `systemd-analyze verify` 不接受 `*.service.next` 临时文件名；改用独立的 `.service` 后缀临时校验路径，失败发生在停止服务前。
- 重新构建静态二进制并确认 `ldd` 为 `not a dynamic executable` 后，安装、启动和健康验证均成功。

## 下一步

- 继续保持 `EVALUATION_ENABLED=false`。
- 若需要生成首条真实 JSONL 审计记录，必须单独批准一次真实评测，并明确实际 `problem_id`、对应版本化用例数量、Provider 调用和 Arena 容器生命周期上限；当前 `problem_id=1` 为 Two Sum v1 的 9 个用例，`problem_id=2` 为 Valid Parentheses v1 的 12 个用例。
- 在取得该批准前，不得将本次审计部署视为长期开放评测或真实 LLM 评测功能完整生产上线。

### 08:58 Task 6：批准后的单次 `problem_id=2` 审计验证

## 执行范围

- 经用户明确选择并批准，执行 1 次有效的 `problem_id=2`（Valid Parentheses v1，12 个版本化用例）生产评测请求；临时启用 `EVALUATION_ENABLED=true`，完成后自动恢复为 `false`。
- 生产 Compose 在 backend 服务中硬编码 `EVALUATION_ENABLED: 'false'`，因此先备份 `/opt/multi-agent-arena/app/compose.yaml` 和 `/opt/multi-agent-arena/app/.env.deploy` 至权限 `700` 的版本化回滚目录，仅临时修改两处开关并只重建 backend；未重建 PostgreSQL，未执行数据库操作。
- 首次请求因多层 shell 转义导致 JSON 载荷损坏，返回 `422`，没有 Provider/评测链路、Arena 容器或审计记录；该请求不计入批准的有效评测次数。
- 随后使用严格 Base64 解码生成的 `{"problem_id":2}` 发起唯一一次有效请求。工具调用超时后通过现场只读核查确认请求已完成，自动恢复流程已执行。

## 审计与恢复结果

- `/var/lib/multi-agent-arena-sandbox-audit/tasks.jsonl` 从 `0` 行增加到 `12` 行，对应 12 个唯一 task ID。
- 12 条记录全部满足：固定 runtime image、`network_mode=none`、只读根文件系统、`/tmp:size=64m,noexec`、`65534:65534`、CPU/内存/swap/PID 限制、`cap_drop=ALL`、`no_new_privileges=true`、5 秒墙钟。
- 12 条记录全部为 `run_outcome=completed`、`exit_code=0`、`cleanup_completed=true`；无 OOM，固定字段全部匹配，未出现候选代码、stdin/stdout/stderr、Provider 内容、凭据、环境变量或其他禁记字段。
- 审计文件权限保持 `640 arena-sandbox:arena-sandbox`；owner 标签容器数为 `0`；controller 为 `active` 且 `/health=200`。
- Compose 和 `.env.deploy` SHA-256 恢复为执行前值：Compose `73f574af561493c6d152ac0346b94441f94831524693642f03500904d5309351`，`.env.deploy` `186d0299d5aa277818bf5ca093f00f15d8fd80543f20a8c554260462bd080a2f`。
- 恢复后 backend `/health=200`，运行时 `EVALUATION_ENABLED=false`，`POST /api/evaluations` 返回 `503`；未保留响应临时文件，因此不对 Judge 最终摘要作超出现场证据的结论。

## 限制与后续

- 本次证明了审计部署、12 个 sandbox 任务的固定运行参数和清理状态；不等同于长期开放评测、强安全隔离、高并发容量或 Provider 费用闭环。
- Provider token/费用记录仍未纳入本次验证；评测入口继续关闭，后续若需长期开放必须重新完成 Task 8 全部门禁和正式验收。

### 09:22 实施计划状态回填与本地回归验证

## 完成内容

- 根据 2026-09-18 和 2026-09-19 已有生产日志证据，回填《2026-09-18-真实LLM与评测生产上线计划.md》中 Task 1、Task 4 和 Task 7 的完成状态。
- 明确 Task 4 已完成：生产入口使用 `36.248.198.60/32` 短期管理白名单，精确保护 `/api/evaluations`，并已保留限流、超时、回滚和关闭路径证据。
- 保留 Task 8 未完成；当前不启用长期评测，`EVALUATION_ENABLED=false` 仍是生产状态。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena
.venv/bin/python -m pytest -q tests/test_deployment_config.py

cd backend
LLM_PROVIDER=mock .venv/bin/python -m pytest -q tests/test_evaluations.py tests/test_agents.py
```

## 验证结果

- 部署静态测试：`9 passed`。
- 后端评测与 Agent 测试：`24 passed`。
- 未执行新的 ECS 写操作、Nginx reload、Provider 调用、真实评测、数据库操作或 Arena 容器创建/删除。

## 遇到的问题

- 本地 `backend/.env` 配置为 OpenAI Compatible Provider；未显式设置 `LLM_PROVIDER=mock` 时，默认 Mock Agent 测试会读取本地 Provider 配置而失败。

## 解决方式

- 测试命令显式使用 `LLM_PROVIDER=mock` 隔离测试配置；未修改用户现有 `.env` 文件，也未在日志中记录其敏感值。

## 下一步

- 继续保持生产评测关闭。
- Task 8 仅在完成长期开放审批、容量与停用策略、异常路径和用户验收后再评估；当前不得报告完整生产上线。

### 09:44 Task 8：按用户确认范围长期开启评测

## 完成内容

- 用户确认正式开放当前全部已配置题目；每题实际用例数由版本化题库决定，当前 `problem_id=1` 为 9 个用例、`problem_id=2` 为 12 个用例。
- 用户确认运行边界：单槽并发、Nginx 评测路由 `1r/m`、单次总超时 `210s`；发生 Provider、controller、超时、资源或清理异常时立即停用评测并回滚到 `503`。
- 通过 Alibaba Cloud Workbench 连接目标 ECS `i-wz95igsjwjrfzpckjip5`，先完成生产基线核验，再将 Compose 与 `.env.deploy` 备份到权限 `700` 的 `/opt/multi-agent-arena/backend-release.h0l5La/task8-enable-20260919-0941`。
- 仅修改评测开关并强制重建 backend；未重建 PostgreSQL，未执行数据库 migration/seed/restore，未修改 Nginx、controller、防火墙或 Docker daemon。

## 执行命令

```bash
workbench list ecs -r cn-shenzhen --status Running -o json
workbench exec -P default -r cn-shenzhen -i i-wz95igsjwjrfzpckjip5 -o json --timeout 150 -c '<脱敏备份、原子替换开关、Compose 校验、仅重建 backend、健康检查命令>'
```

## 验证结果

- 开启后 backend 为 `running/healthy`，运行时 `EVALUATION_ENABLED=true`。
- controller 为 `active`，私网 `/health=200`；backend `/health=200`；`/api/problems=200`。
- Nginx `nginx -t` 通过；评测开启模式监控返回 `monitor_ok checked=backend,postgres,http,sandbox,disk,backup,logs`。
- Arena owner 标签容器数量为 `0`；本次开启动作未主动调用 Provider、未发送真实评测请求、未创建或删除 Arena 容器。
- backend 仍只监听 `127.0.0.1:8000`，controller 仍只监听 `172.30.0.1:8001`。

## 遇到的问题

- 第一次开启命令在 Docker inspect 模板验证阶段因转义错误退出；自动回滚已恢复 Compose 与 `.env.deploy` 原哈希，并验证 backend healthy、`EVALUATION_ENABLED=false`、`/health=200`、`/api/problems=200`、controller 健康和无 Arena 容器残留。

## 解决方式

- 使用不依赖该 Docker inspect 模板的修正版命令重新执行；第二次仅重建 backend 成功，保留权限 `700` 回滚材料。
- Task 8 计划已回填为当前范围的正式开启状态；同时保留限制：Provider 上游账户配额、token/费用观测和费用上限仍未闭环，不宣称强隔离、高可用或无限容量。

## 下一步

- 评测现已按用户确认范围长期开启；后续任何真实评测请求仍需遵守版本化用例数、单槽、`1r/m`、`210s` 和异常立即停用规则。
- 发生异常时执行既有回滚流程：恢复 `EVALUATION_ENABLED=false`、仅重建 backend、验证 `/health`、`/api/problems` 和 `/api/evaluations=503`，并核查 Arena owner 容器为 `0`。