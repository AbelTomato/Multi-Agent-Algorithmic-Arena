# 2026-09-20

## 完成内容

- 经明确授权，删除旧 Python/FastAPI sandbox 控制器、专属配置及 Python 测试；Go 控制器仍为唯一正式执行入口。
- 更新 sandbox 文档，明确 Python 候选程序支持和运行时镜像仍被保留。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
ARENA_SANDBOX_INTEGRATION=0 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./...
/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go vet ./...
```

## 验证结果

- 删除前和删除后 Go 非 Docker 测试、`go vet` 通过。
- 未运行会创建和清理 Docker 容器的真实集成测试。

## 遇到的问题

- 阶段 A 删除属于不可逆文件操作，需要针对精确清单的明确授权。

## 解决方式

- 完成工作区状态、活动引用和 Go 基线检查后，取得针对 10 个文件的明确删除授权，再逐文件删除。

## 下一步

- 在业务 Spec 未确认前，阶段 B 仅可继续协议与现状分析；不得实现比赛状态机或未冻结的多语言准入。

## 2026-09-20 13:28（Asia/Shanghai）

## 完成内容

- 完成阶段 B 执行协议替换：`POST /execute` 严格使用 `execution-api-v2`、`runtime_id`、`source`、`stdin_input`、`io_protocol`，拒绝旧字段、未知字段、缺失字段、未知 runtime 和不支持的 I/O 协议。
- 新增受信任 Python runtime Registry；镜像、命令、资源限制和 `json-stdio-v1` 支持由 Registry 提供，Runner 不接受调用方 Docker 参数。
- 更新 Python `SandboxClient`、Go 测试、集成请求样例和 sandbox 文档。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./cmd/... ./internal/...
/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go vet ./cmd/... ./internal/...

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
python3 -m compileall -q app tests/test_judges_stage2.py
python3 -m pytest tests/test_judges_stage2.py -q
```

## 验证结果

- Go 非 Docker 目标包测试通过，`go vet` 通过。
- Python 语法检查通过；Judge pytest 因环境缺少 `pytest` 模块未执行成功。
- 未运行 Docker 集成测试，未执行数据库、部署或 Git 提交。

## 遇到的问题

- 初次迁移测试发现 Runner 仍引用旧全局镜像常量，且未将 Registry 的 `ReadOnly` 配置映射到 Docker 参数。
- 当前 Python 环境没有 `pytest` 命令模块。

## 解决方式

- 将审计和测试改为从 Registry 读取 runtime 配置；Runner 按 Registry 注入的资源配置生成参数，并补充未知 runtime/I/O 协议的前置拒绝测试。
- 使用 `python3` 完成语法验证，保留 pytest 未执行状态。

## 下一步

- 在具备 pytest 依赖的环境运行 `backend/tests/test_judges_stage2.py`；取得授权并具备 Docker 环境后再单独运行真实集成测试。

## 2026-09-20 14:42（Asia/Shanghai）

## 完成内容

- 使用现有 `backend/.venv` 完成阶段 B Judge/Client pytest 验证；阶段 B 的 Go 非 Docker 目标测试、`go vet`、Python 语法检查均通过。
- 校正实施计划状态：`POST /execute` 是严格 v2 协议替换，旧 `code` 与 `protocol_version` 字段被拒绝；兼容范围是已迁移的 Python `SandboxClient`，不是旧 HTTP 请求。
- 阶段 B 标记为完成；当前仅注册 Python runtime，不宣称第二语言已经支持。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
ARENA_SANDBOX_INTEGRATION=0 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./cmd/... ./internal/...
ARENA_SANDBOX_INTEGRATION=0 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go vet ./cmd/... ./internal/...

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend/.venv/bin/python -m compileall -q app tests/test_judges.py tests/test_judges_stage2.py
LLM_PROVIDER=mock ARENA_SANDBOX_INTEGRATION=0 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend/.venv/bin/python -m pytest -q tests/test_judges.py tests/test_judges_stage2.py
```

## 验证结果

- Go `./cmd/... ./internal/...` 全部通过；`go vet ./cmd/... ./internal/...` 通过。
- Python 语法检查通过；Judge/Client pytest 为 **45 passed**。
- pytest 输出 147 条现有 `pytest-asyncio` 事件循环弃用警告和 `TestCase` 收集警告；没有测试失败。
- 未运行真实 Docker 集成测试，未执行数据库、真实模型请求、部署或 Git 提交。

## 遇到的问题

- 项目的固定目标测试工具未接受当前 Go/pytest 路径，返回工具层错误，未产生测试结果。

## 解决方式

- 改用计划中列出的等价、限定范围的 Go 和 `backend/.venv` pytest 命令完成验证。

## 下一步

- 阶段 C 前需要用户明确第二种语言及精确版本、固定镜像 digest、预置依赖、编译/运行资源预算，并单独授权真实 Docker 集成测试。
- 阶段 D 前需要确认身份授权、源码存储与保留期限、提交修订和接口兼容 Spec；不得因阶段 B 完成而直接实现比赛或持久化模型。

## 2026-09-20 15:36（Asia/Shanghai）

## 完成内容

- 完成阶段 C 的 Go 控制器侧 C++ 支持：新增受信任 `cpp-gcc-14-cpp20-v1` runtime，固定 GCC 14/C++20 命令 `g++ -std=c++20 -O2 -pipe`，并使用经核验的 `m.daocloud.io/docker.io/library/gcc@sha256:cb57ac6c7917425c057c736fe3a240df25bce310418d61cd223bc8e411364876`。
- 实现隔离的“初始化受控 volume → 非 root 编译 → 只读挂载产物运行”生命周期。编译限制为 15 秒、512 MiB、1 CPU、32 PID 和 64 KiB 合并输出；运行使用既有 5 秒、128 MiB 和 64 KiB 限制。
- 增加编译结果语义、产物可执行性检查、双标签 volume 创建/删除前核验，以及从受信任 Docker 运行参数反查 runtime 的审计记录，避免 C++ 任务被错误审计为 Python。
- 更新 sandbox README 和实施计划。Python 后端 Client/Judge 仍固定 Python runtime；C++ 的提交、语言选择、题目语言准入与 Checker 统一语义留待阶段 D Spec。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
ARENA_SANDBOX_INTEGRATION=0 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./cmd/... ./internal/...
ARENA_SANDBOX_INTEGRATION=0 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go vet ./cmd/... ./internal/...
ARENA_SANDBOX_INTEGRATION=1 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./tests/integration -run '^TestRunnerRealDockerLifecycle$' -v -count=1
ARENA_SANDBOX_INTEGRATION=1 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./tests/integration -run '^TestRunnerRealDockerCppCompilationLifecycle$' -v -count=1
```

## 验证结果

- DaoCloud 镜像拉取完成，`docker image inspect` 核验为 `os=linux`、`arch=amd64` 和上述固定 digest。
- fake Docker 测试覆盖成功编译、编译错误、编译超时、编译输出超限、产物缺失、基础设施失败、受控 volume 清理和 C++ 审计镜像；Go `./cmd/... ./internal/...` 及 `go vet` 通过。
- 真实 Docker 验收通过：Python 生命周期（完成、非零退出、运行超时、输出上限）与 C++ 生命周期（合法 C++20、编译错误、运行超时）均通过；每次测试后无 Arena owner 标签容器或 volume 残留。
- 未执行数据库、真实模型请求、部署或 Git 提交。

## 遇到的问题

- Docker Hub 的 `gcc:14` 拉取停滞，已按授权仅终止该次拉取；改用项目现有 DaoCloud Docker Hub 代理并在本地核验 digest。
- 初始化容器继承 `cap-drop ALL` 后缺少 `CAP_CHOWN`，无法将产物 volume 交给非 root 编译用户；外层组合真实测试又因工具 30 秒上限被中断，遗留一对该测试创建的 Arena 标签资源。

## 解决方式

- 初始化容器保持只读 root 和 `cap-drop ALL`，仅额外授予 `CAP_CHOWN`；编译/运行容器不增加 capability。对遗留资源先核验容器名称、owner/task-id 双标签及 volume 双标签，再仅删除该精确匹配的一对资源；后续将真实 Python/C++ 验收拆分执行。

## 下一步

- 在阶段 D Spec 确认后，定义 Submission 的语言/源码契约与权限、存储和保留策略，接入 C++ 选择，并验证同一题目 I/O 与 Checker 对 Python/C++ 的一致语义；数据库 migration 或任何持久化写入须单独审批。

## 2026-09-20 19:04（Asia/Shanghai）

## 完成内容

- 经用户明确授权直接在 `main` 分支实施阶段 D 核心代码：账户主体与 Bearer Token、题目语言白名单和当前用例版本、不可变 Submission、结构化 CandidateGeneration、纯 Submission Evaluation、源码保留清理和新 Submission API。
- 移除旧匿名 `POST /api/evaluations` 创建入口，保留历史评测只读查询；新增隔离 SQLite 的“候选生成 → 提交 → 纯评测”集成测试，并迁移旧匿名创建断言。
- 回填阶段 D Spec、实施计划索引和未完成项说明；明确真实 Sandbox 等价 Python/C++ 验收及重启幂等场景尚未覆盖。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m compileall -q app tests
LLM_PROVIDER=mock ARENA_SANDBOX_INTEGRATION=0 .venv/bin/python -m pytest -q \
  tests/test_auth_subjects.py \
  tests/test_submissions.py \
  tests/test_candidate_generation.py \
  tests/test_submission_evaluations.py \
  tests/test_submission_api.py \
  tests/test_stage_d_migration.py \
  tests/test_problem_model.py
.venv/bin/python -m pytest -q \
  tests/integration/test_evaluation_flow.py \
  tests/integration/test_submission_evaluation_flow.py
```

## 验证结果

- `compileall` 通过。
- 阶段 D 目标测试：29 passed；无失败。
- 集成测试：2 passed、1 skipped；skip 为真实 Go Sandbox 控制器测试，因 `ARENA_SANDBOX_INTEGRATION=0`。
- 未执行持久 PostgreSQL migration、seed、账户写入、部署、真实模型请求或未经批准的 Docker 外部操作。

## 遇到的问题

- 旧集成测试仍直接调用已删除的匿名 `POST /api/evaluations`，首次运行返回 405。
- 新 API 测试初始依赖覆盖函数缺少 FastAPI `Depends(get_db)`，导致依赖解析错误。

## 解决方式

- 将旧测试迁移为断言匿名创建入口返回 405 且不会产生历史记录；保留历史 GET 只读断言。
- 修正测试依赖覆盖并确认新 Submission API 目标测试通过。

## 下一步

- 后续若继续阶段 D 验收，需要在单独授权和具备控制器环境时补充真实 Python/C++ 等价 Checker 集成、服务重启/重复查询幂等覆盖；持久数据库 migration 仍须单独审批后执行。
