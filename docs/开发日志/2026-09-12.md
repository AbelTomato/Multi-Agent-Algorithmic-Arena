# 2026-09-12

## 今日目标

- 整理当前工作区改动并按功能边界拆分 Git commit，为迁移到 WSL 做准备。

## 完成内容

- 核对部署资产、项目文档、历史开发日志和本地未跟踪文件。
- 确认 `frontend/.env.local.devbackup` 是本地环境备份，不纳入 Git。
- 按部署资产、部署文档、项目状态文档拆分提交。

## 执行命令

```powershell
git diff --check
..\.venv\Scripts\python.exe -m pytest -q
..\.venv\Scripts\python.exe -m compileall -q app alembic tests
npm test -- --run
npm run build
```

## 验证结果

- 后端：29 passed，1 skipped；
- 前端：8 passed；生产构建通过；
- Python 编译检查和 `git diff --check` 通过；
- 未执行数据库 migration、seed 或其他外部环境写操作。

## 下一步

- 迁移到 WSL 后重新安装 Python/npm 依赖，并复跑后端、前端测试和构建。
- 继续完成正式域名 HTTPS、凭据轮换、备份恢复和真实 Provider 选择。