# 2026-09-14

## 今日目标

- 准备 Nginx 公网 IP 灰度模板，加入 `/api/solutions` 限流配置。

## 完成内容

- 新增 `deploy/nginx/multi-agent-arena-gray.conf`，使用 `server_name _` 和 HTTP `80` 默认站点作为公网 IP 灰度入口。
- 配置 `solution_by_ip` 限流 zone：`2r/min`，`burst=2`，超限返回 HTTP `429`。
- 在灰度模板中保留前端静态目录、`/api/`、`/health`、静态文件路由、安全响应头和代理超时。
- 在正式 HTTPS 模板中预置同一限流 zone 和精确 `/api/solutions` location。
- 将正式模板示例域名和证书路径统一为 `tomato-agent-arena.me`。
- 未部署灰度模板，未修改 ECS、安全组或 Nginx 运行时配置。

## 执行命令

```bash
grep -n "limit_req\|server_name\|127.0.0.1:8000\|proxy_read_timeout" \
  deploy/nginx/multi-agent-arena-gray.conf \
  deploy/nginx/multi-agent-arena.conf
.venv/bin/python - <<'PY'
# 对两个模板执行关键 directive 的结构化静态检查
PY
git diff --check
```

## 验证结果

- 灰度和正式模板关键 directive 结构化检查：通过。
- `git diff --check`：通过。
- 本机未安装 `nginx` 命令，未能执行真实 `nginx -t`；不能将结构化检查等同于 Nginx 语法验证。
- 未执行 ECS 部署、`systemctl reload nginx`、安全组修改或公网限流验证。

## 遇到的问题

- 当前开发环境没有安装 `nginx` 可执行文件。

## 解决方式

- 使用计划范围内的字段检查和 Python 结构化检查验证模板内容，并将真实 `nginx -t` 留到目标 ECS 部署前执行。

## 学到的东西

- `limit_req_zone` 必须位于 Nginx 的 `http` 配置上下文；站点模板作为 include 文件时，可以放在 server block 之前的顶层。
- 精确的 `/api/solutions` location 需要保留完整的 proxy header 和超时配置，不能只添加限流 directive。

## 仍然不理解的问题

- 无

## 下一步

- 阶段 5 进入 ECS 灰度入口收口前，先取得当前操作者公网 IPv4 `/32`、目标安全组、SSH 恢复规则、Nginx 文件和回滚方式的明确批准。

## 前端页面职责拆分

### 完成内容

- 将 `frontend/src/App.tsx` 收缩为页面布局和组件组合，移除题目请求、解题请求和错误文案映射等混合逻辑。
- 新增 `useProblemExplorer`，集中管理题目列表、题目详情和解题请求状态。
- 将题目列表、侧栏、题目详情、解题结果和通用加载/空状态拆分为独立组件。
- 为解题请求增加 `AbortSignal`，切换题目时取消旧请求并忽略过期响应，避免旧题目解法污染当前页面。
- 增加切换题目期间忽略旧解法响应的前端回归测试。

### 执行命令

```bash
cd frontend && npm test
cd frontend && npm run build
```

### 验证结果

- 前端 Vitest：2 个测试文件、10 个测试全部通过。
- 前端 TypeScript 检查和 Vite 生产构建：通过。

## 阶段 5：ECS 灰度入口收口

### 执行环境

- ECS：`47.119.120.86`
- 操作者公网 IPv4：`112.48.20.179/32`
- 安全组已将 TCP/80 限制为 `112.48.20.179/32`，SSH 恢复规则保留。

### 完成内容

- ECS `docker compose config --quiet` 通过。
- backend 和 PostgreSQL 容器均为 `healthy`。
- backend 仅绑定 `127.0.0.1:8000`，PostgreSQL 未映射宿主机端口。
- backend 未发现 `HTTP_PROXY`、`HTTPS_PROXY` 或 `ALL_PROXY`；`.env.deploy` 权限为 `600`。
- Nginx 当前配置已备份至 `/opt/multi-agent-arena/nginx-backups/20260914-092416/multi-agent-arena.conf`。
- Nginx 主配置已备份至 `/opt/multi-agent-arena/nginx-backups/20260914-092416/nginx.conf`。
- 灰度模板已安装至 `/etc/nginx/conf.d/multi-agent-arena.conf`。
- 发现 Alibaba Cloud Linux Nginx 不接受 `rate=2r/min`，ECS 配置和仓库模板已统一为兼容语法 `rate=2r/m`。
- Nginx 默认 server 与灰度模板的 `server_name _` 冲突，已将默认 server 改为 `default.invalid`。

### 验证结果

- `sudo nginx -t`：通过。
- `sudo systemctl reload nginx`：成功，Nginx 状态为 `active`。
- `/health`：HTTP `200`。
- `/api/problems`：HTTP `200`。
- `/api/solutions` 使用无效请求体 `{}` 验证限流，返回：`422、422、422、429、429`。
- 前 3 个请求由 FastAPI 参数校验返回 `422`，后 2 个请求由 Nginx 限流返回 `429`。
- 限流验证未发送真实解题请求，不产生 Provider 调用。

### 回滚点

- Nginx 站点配置：`/opt/multi-agent-arena/nginx-backups/20260914-092416/multi-agent-arena.conf`
- Nginx 主配置：`/opt/multi-agent-arena/nginx-backups/20260914-092416/nginx.conf`
- 回滚前需恢复对应配置，重新执行 `sudo nginx -t`，确认通过后再 reload。

### 未完成事项

- 尚未完成专用 API Key、额度/费用告警、持续监控、异地备份、隔离恢复和正式 HTTPS 切换。

### 下一步

- 阶段 6 已完成，真实 Provider 灰度链路已验证。

## 阶段 6：专用凭据和真实 Provider 收口

### 完成内容

- Provider 专用灰度 Key、模型范围、日/月额度和费用告警已按灰度要求配置完成。
- 此前可能暴露的旧 Key 已完成轮换或撤销。
- ECS `/opt/multi-agent-arena/app/.env.deploy` 已通过受保护方式更新，真实 Key 未进入命令文本、日志、代码或镜像。
- `LLM_PROVIDER=openai_compatible`、Provider Base URL、模型、超时、最大 Token 和公网 IP 灰度 CORS 配置已生效。
- `.env.deploy` 权限保持为 `600`。
- backend 已完成重建，PostgreSQL 数据和 volume 未变更。

### 验证结果

- Provider `/v1/models`：HTTP `200`。
- backend 重建后健康检查：HTTP `200`。
- 受控真实 `POST /api/solutions` 请求：HTTP `200`。
- 请求耗时：`234ms`。
- 响应字段：`language`、`problem_id`、`result`。
- 解题结果长度：`299`。
- 未记录 API Key、Authorization、Prompt、解题 Markdown 或完整上游响应。

### 阶段状态

- 阶段 6 已收口，ECS 公网 IP → Nginx → FastAPI → OpenAI Compatible Provider → Markdown 结果的 MVP 灰度链路已完成一次受控验证。
- 阶段 7 已完成错误、日志和运行观察检查。
- 持续监控、异地备份、隔离恢复和正式 HTTPS 切换仍属于后续明确缺口。

## 阶段 7：错误、日志和运行观察

### 本地验证

- Provider、Solutions API 和 CORS 目标测试：`24 passed`。
- Provider 测试覆盖 HTTP `429/500`、传输超时、非法 JSON、空内容、错误脱敏和环境代理隔离。
- Solutions API 测试覆盖重试耗尽后的受控 `502` 响应。
- 前端 App 和 API 客户端目标测试：`10 passed`。
- 未访问真实 Provider，未消耗额外 Provider 配额。

### ECS 日志检查

- backend 最近 30 分钟敏感字段检查：`clear`。
- Nginx error log 敏感字段检查：无匹配。
- Nginx access log 命中 `api_key`/`secret` 关键词共 4 条，但均为 GET 未知路径，未命中 `/api/solutions`、`/api/problems` 或 `/health`，且无查询参数、Referer 或 User-Agent 命中。
- 上述 4 条未知路径请求均返回 HTTP `200`，结合 SPA fallback 判断为外部扫描路径被前端入口返回，不是 API Key 泄露证据；未输出原始日志内容。
- 当前运行观察仅完成日志敏感字段和 HTTP 状态聚合，未发现 Authorization、Bearer、密码或数据库连接串。

### 运行保障缺口

- 当前没有持续监控系统，尚未具备完整的自动告警、调用量、Provider 错误率、CPU、内存和费用监控能力。
- 后续需补充异地备份、隔离恢复演练和正式域名 HTTPS 切换。

### 下一步

- 进入阶段 8，先确认异地备份目标、保留周期、加密方式、访问范围、临时恢复环境和清理方式的明确批准。

## 阶段 8：异地备份与对象完整性

### 完成内容

- 已确认 ECS 位于 `cn-shenzhen`，实例为 `i-wz95igsjwjrfzpckjip5`。
- 用户已创建杭州 OSS 私有 Bucket：`abeltomato-algorithmic-agent-arena-bucket`。
- Bucket 服务端加密采用 OSS 完全托管，未使用 KMS；版本控制已开启。
- 已配置数据库备份前缀的生命周期：当前版本和非当前版本均为 7 天后清理。
- ECS 已创建并绑定 RAM Role：`multi-agent-arena-backup-role`；权限范围限定为目标 Bucket 的 `multi-agent-arena/postgres/` 前缀对象读写，不授予删除对象或修改 Bucket 配置权限。
- ECS 已安装 `ossutil 2.4.0`，上传使用 `EcsRamRole` 认证，未写入长期 AccessKey Secret。
- 已将 PostgreSQL 备份上传至：`multi-agent-arena/postgres/multi-agent-arena-2026-09-13-172804.sql.gz`。

### 执行命令

```bash
ossutil cp /opt/multi-agent-arena/backups/multi-agent-arena-2026-09-13-172804.sql.gz \\
  oss://abeltomato-algorithmic-agent-arena-bucket/multi-agent-arena/postgres/multi-agent-arena-2026-09-13-172804.sql.gz \\
  --mode EcsRamRole --region cn-hangzhou

ossutil cp oss://abeltomato-algorithmic-agent-arena-bucket/multi-agent-arena/postgres/multi-agent-arena-2026-09-13-172804.sql.gz - \\
  --mode EcsRamRole --region cn-hangzhou --quiet | sha256sum
```

### 验证结果

- ECS 本地备份复核：权限 `600`，大小 `1380 bytes`，gzip 完整性通过。
- 本地备份 SHA256：`0e9ac9a3e284b38acb5906aaef6feab9f8f09d4b15f6a4603f3996cee7e18e0d`。
- OSS 上传：成功，`1` 个对象，大小 `1380 B`。
- 从 OSS 读取对象并计算 SHA256：`0e9ac9a3e284b38acb5906aaef6feab9f8f09d4b15f6a4603f3996cee7e18e0d`，与本地一致。
- OSS 对象大小复核：`1380 bytes`，通过。
- `ossutil stat` 读取对象 ACL 时返回 `403 AccessDenied`，原因是当前最小权限未授予 `GetObjectAcl`；对象内容读取和完整性校验均成功，未扩大权限。

### 当前缺口

- 尚未执行 PostgreSQL 隔离恢复演练；恢复前需要单独批准，只允许恢复到临时隔离 PostgreSQL，禁止触碰生产 `postgres_data` volume。
- 持续监控、自动告警和正式域名 HTTPS 仍未完成。


## 阶段 8：隔离 PostgreSQL 恢复演练

### 完成内容

- 经明确批准，在 ECS `i-wz95igsjwjrfzpckjip5` 上创建临时 PostgreSQL 容器：`arena-restore-20260914`。
- 临时容器使用 Docker `--network none`，未开放端口，未连接生产 Compose 网络。
- 临时恢复数据使用命名卷：`arena_restore_20260914`；生产卷 `app_postgres_data` 未挂载到临时容器。
- 从杭州 OSS 读取已校验备份，并成功导入临时 PostgreSQL。
- 恢复后只读验证 Alembic 版本和题目数量。

### 验证结果

- 临时数据库恢复：成功。
- `SELECT version_num FROM alembic_version;`：`0001`。
- `SELECT count(*) FROM problems;`：`2`。
- 生产 backend 和 PostgreSQL 容器仍为 `healthy`。
- 生产 PostgreSQL 仍使用 `app_postgres_data`，与临时恢复卷分离。
- 临时容器当前状态为 `running`。

### 临时资源与清理边界

- 临时容器：`arena-restore-20260914`。
- 显式命名卷：`arena_restore_20260914`。
- PostgreSQL 镜像默认 `VOLUME /var/lib/postgresql/data` 另外生成匿名卷：`c73dd96fd71988f9d95eb778c43a7572ceb909530efca7b0813114f85db9d1d8`。
- 上述临时容器和两个临时卷尚未清理；清理需要单独批准，且不得删除生产卷 `app_postgres_data`。


### 临时资源清理结果

- 经明确批准，已删除临时容器 `arena-restore-20260914`。
- 已删除命名卷 `arena_restore_20260914` 和 PostgreSQL 自动生成的匿名临时卷。
- 删除前安全检查确认临时容器未挂载生产卷 `app_postgres_data`。
- 删除后确认生产卷 `app_postgres_data` 仍存在。
- 清理后生产 backend 和 PostgreSQL 容器仍为 `healthy`。
- 阶段 8 的 OSS 异地备份、完整性验证、隔离恢复和临时资源清理均已完成。


## 阶段 9：应用级回滚路径只读核对

### 核对结果

- ECS 当前生产 Compose 使用 `build:`，backend 服务未在 Compose 文件中固定 `image:` 标签。
- 当前运行中的 `app-backend-1` 使用镜像 ID：`sha256:599dc32a9a76b0a70051a53fd03e87f05e3ca738d07fef4957e5ea8a9268948a`。
- 当前运行镜像同时具有标签 `app-backend:latest` 和 `app-backend:rollback-2026-09-13-183317`，创建时间为 `2026-09-10T16:05:55+08:00`。
- 候选镜像 `app-backend:412ba74` 的镜像 ID 为：`sha256:67b6b0f56ac288ff10a18e5cc0f9fb5555998d41874dc00443e8dadc99021804`，创建时间为 `2026-09-13T17:00:40+08:00`，当前未被运行容器使用。
- `.env.deploy` 及既有环境文件备份存在，权限为 `600`。
- Nginx 站点配置和主配置备份存在于 `/opt/multi-agent-arena/nginx-backups/20260914-092416/`。
- PostgreSQL 本地备份存在，权限为 `600`，大小为 `1380 bytes`，SHA256 与已上传 OSS 对象一致。
- 当前 backend 和 PostgreSQL 容器均为 `healthy`；本机 Nginx `/health` 和 `/api/problems` 均返回 HTTP `200`。

### 风险与边界

- 当前事实不能支持将 `app-backend:412ba74` 标记为已上线版本；它仍是 ECS 上保留的候选镜像。
- 当前未执行 backend 重建、重启、镜像切换或生产配置修改。
- 如需将候选镜像切换为运行版本，必须单独批准生产 backend 变更，并在切换前确认旧容器、旧镜像、`.env.deploy`、数据库和 Nginx 回滚点均保留。
- 阶段 9 已完成回滚资料的只读核对，但候选镜像上线状态存在不一致，暂不宣称应用版本切换验收完成。

### 阶段 9 生产切换结果

- 经明确批准，使用临时 Compose override 将生产 backend 切换到 `app-backend:412ba74`，仅重建 backend，未重启 PostgreSQL。
- 切换后 `app-backend-1` 实际镜像 ID 为：`sha256:67b6b0f56ac288ff10a18e5cc0f9fb5555998d41874dc00443e8dadc99021804`。
- 旧镜像回滚标签仍保留：`app-backend:rollback-2026-09-13-183317`，对应镜像 ID：`sha256:599dc32a9a76b0a70051a53fd03e87f05e3ca738d07fef4957e5ea8a9268948a`。
- 切换后 backend 和 PostgreSQL 均为 `healthy`，生产卷 `app_postgres_data` 仍存在且未被替换。
- 切换后 `/health` 返回 HTTP `200`，`/api/problems` 返回 HTTP `200`。
- 已发送一次真实 Provider 请求：Nginx 访问日志记录 `POST /api/solutions` 返回 HTTP `200`，响应体大小为 `2267` 字节；backend 日志同步记录 `200 OK`。
- 本次调用后的本地响应解析脚本存在 f-string 语法错误，且临时响应文件已按清理逻辑删除，因此未采集响应字段名和 `result` 长度；未因该缺口重试 Provider 请求。
- 本阶段未执行数据库 migration、restore、downgrade、生产卷删除、Nginx reload 或配置凭据修改。

### 阶段 9 当前结论

- 应用级镜像切换和基础健康验证完成。
- 真实 Provider 请求已得到 HTTP 层成功证据，但业务响应字段完整性未完成独立采集，Provider 验收保留上述测试缺口。
- 若后续发现 Provider 或应用异常，可优先保留数据库和生产卷不变，使用已保留的旧镜像回滚；任何回滚操作仍需单独确认操作范围。

## 自动备份闭环材料

### 已完成

- 新增 PostgreSQL 自动备份脚本：`deploy/backup/multi-agent-arena-postgres-backup.sh`。
- 新增 systemd service 和 timer 模板：`deploy/backup/multi-agent-arena-postgres-backup.service`、`deploy/backup/multi-agent-arena-postgres-backup.timer`。
- 新增自动备份方案文档：`docs/实施计划/2026-09-14-自动备份方案.md`。
- 脚本设计为从生产 Compose 的 `postgres` 服务执行 `pg_dump`，gzip 校验后上传杭州 OSS，并重新读取 OSS 对象比对 SHA256。
- 脚本不保存长期凭据、不自动删除本地历史备份、不删除 OSS 对象、不执行数据库写入或卷操作。

### 当前边界

- 上述文件已在仓库中完成，并已按明确批准安装到 ECS。
- ECS 已创建 `/etc/multi-agent-arena/backup.env`，权限为 `600`；未写入 AccessKey、Secret、STS Token 或数据库密码。
- ECS 已执行 `systemctl daemon-reload`，并已启用、启动 `multi-agent-arena-postgres-backup.timer`。
- 上传到 ECS `/tmp` 的三个安装源文件暂未删除，避免未经单独确认执行远程删除操作。

### 本地验证结果

- 备份脚本 `bash -n`：通过。
- timer 的 `OnCalendar=*-*-* 03:15:00 Asia/Shanghai`：通过 `systemd-analyze calendar` 校验。
- 使用隔离 mock `docker` 和 `ossutil` 完整执行脚本成功，覆盖 `pg_dump`、gzip、上传、OSS 回读和 SHA256 比对。
- mock 验证确认备份文件权限为 `600`、备份目录权限为 `700`，本地和远端校验和一致。
- 本地 `systemd-analyze verify` 无法完成生产 unit 的依赖验证，原因是工作站没有生产 ECS 的 `docker.service`、`/usr/local/sbin/multi-agent-arena-postgres-backup` 和 `/etc/multi-agent-arena/backup.env`；这三个路径将在获批安装后由 ECS 提供，不代表 unit 文本语法错误。

### ECS 安装与首次执行结果

- 备份脚本已安装至 `/usr/local/sbin/multi-agent-arena-postgres-backup`，权限为 `750`。
- systemd service 和 timer 已安装至 `/etc/systemd/system/`，权限均为 `644`。
- `multi-agent-arena-postgres-backup.timer`：`enabled`、`active`；下一次计划执行为 `2026-09-15 03:19:23 CST`，包含不超过 10 分钟的随机延迟。
- 首次手动执行 `multi-agent-arena-postgres-backup.service`：`Result=success`，`ExecMainStatus=0`。
- 首次备份对象：`multi-agent-arena/postgres/multi-agent-arena-2026-09-14-132308.sql.gz`。
- 首次备份大小：`1380 bytes`；本地 gzip 校验通过。
- 首次备份 SHA256：`a75224d2aaa131f3b465966c3aafd0f94050abb58bcb8926c83e5d30d1658ca0`；service 日志已记录 OSS 回读校验成功。
- 首次执行后 backend 和 PostgreSQL 仍为 `running/healthy`，生产卷 `app_postgres_data` 未变更。

## 最小监控材料

### 已完成

- 新增运行检查脚本：`deploy/monitor/multi-agent-arena-monitor.sh`。
- 新增 systemd service 和 timer 模板：`deploy/monitor/multi-agent-arena-monitor.service`、`deploy/monitor/multi-agent-arena-monitor.timer`。
- 新增监控方案文档：`docs/实施计划/2026-09-14-监控与告警方案.md`。
- 检查范围包括 backend/PostgreSQL 容器 health、本机 `/health` 和 `/api/problems`、`/opt` 磁盘使用率、备份新鲜度与 gzip 完整性、备份 service 结果，以及 backend/Nginx 日志敏感字段模式。
- 检查失败只输出检查名称并返回非零状态，不输出日志匹配内容、数据库内容、Prompt、API Key 或 Authorization。

### 本地验证结果

- Shell 语法和 timer 日历表达式：通过。
- 隔离 mock 成功路径：通过，输出 `monitor_ok`，退出码 `0`。
- 隔离 mock 失败路径：通过，backend/PostgreSQL health 异常时输出 `monitor_failed`，退出码 `1`。
- 失败输出未泄露日志原文或敏感字段值。
- `git diff --check`：通过。

### 当前边界

- 监控脚本和 systemd 模板已安装到 ECS，监控 timer 已启用并运行。
- 尚未配置主动告警通知渠道；当前方案只能通过 systemd/journald 暴露失败状态，不能宣称已具备自动通知能力。
- 通知渠道接入还需要先确定渠道、凭据存储、接收人和频率限制。


### ECS 安装与验证结果

- 经明确批准，监控脚本已安装至 `/usr/local/sbin/multi-agent-arena-monitor`，权限为 `750`。
- 监控配置已写入 `/etc/multi-agent-arena/monitor.env`，权限为 `600`；仅包含路径、阈值和日志窗口，不包含秘密。
- systemd service 和 timer 已安装至 `/etc/systemd/system/`，权限均为 `644`。
- `multi-agent-arena-monitor.timer` 已 `enabled`、`active`，每 5 分钟执行一次；下一次检查为 `2026-09-14 13:46:29 CST`。
- 首次监控检查因 Nginx access log 中的 `/secrets.json` 和 `/api_keys.json` 扫描路径触发过宽敏感关键词规则，返回失败；逐项核对确认 backend、PostgreSQL、HTTP、磁盘和备份均正常。
- 已将 Nginx access log 检查收窄为疑似真实凭据值，普通扫描路径不再触发误报；backend 日志和 Nginx error log 仍保留敏感字段检查。
- 修正后再次执行 monitor service：`Result=success`，`ExecMainStatus=0`，日志输出 `monitor_ok checked=backend,postgres,http,disk,backup,logs`。
- 修正后确认 backend/PostgreSQL 均为 `running/healthy`，备份 service 仍为 `Result=success`，生产卷 `app_postgres_data` 未变更。
- 在邮件组件部署前，监控仅通过 systemd/journald 记录失败；邮件告警组件随后已完成本地验证并部署到 ECS，真实 SMTP 凭据仍未配置。
- 上传到 ECS `/tmp` 的监控安装源文件仍保留，未执行远程删除。

## 邮件告警组件

### 已完成

- 新增 SMTP SSL 发信器：`deploy/monitor/multi-agent-arena-monitor-mail.py`，仅使用 Python 标准库。
- 新增监控告警包装器：`deploy/monitor/multi-agent-arena-monitor-alert.sh`，支持首次故障立即通知、持续故障每小时限频和恢复通知。
- monitor service 已改为调用告警包装器，保留原始监控退出码；SMTP 未配置或发送失败不会掩盖监控失败。
- 邮件正文只包含主机、时间、状态和脱敏检查名称，不包含完整日志、Prompt、API Key、Authorization 或 Provider 响应。
- QQ SMTP 使用 `smtp.qq.com:465` SSL；授权码不写入仓库、命令参数或日志。

### 当前边界

- 在邮件组件初次部署阶段尚未在 ECS 创建 `/etc/multi-agent-arena/mail.env`，当时也未发送真实邮件；后续已由用户通过受保护会话创建该文件，当前仅核对到权限和元数据，尚未读取配置内容。
- 聊天中出现过的授权码按已暴露凭据处理，正式接入必须使用 QQ 邮箱后台新生成的授权码。
- 邮件发送失败目前只进入 journald；告警组件本身不改变监控 service 的退出状态。

### ECS 部署与验证结果

- 已经明确批准后，通过 Workbench CLI 将告警包装器、SMTP SSL 发信器和更新后的 monitor service 部署到 `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）。
- monitor service 已切换至 `/usr/local/sbin/multi-agent-arena-monitor-alert`，并设置 `StateDirectory=multi-agent-arena-monitor`。
- `multi-agent-arena-monitor.timer` 仍为 `enabled`、`active`，下一次执行已正常排定。
- 部署后手动执行 monitor service 成功：`Result=success`、`ExecMainStatus=0`；日志仍输出 `monitor_ok checked=backend,postgres,http,disk,backup,logs`。
- ECS 文件权限核对通过：告警包装器 `750`、邮件脚本 `755`、service `644`、状态目录 `700`。
- `/etc/multi-agent-arena/mail.env` 当前存在，权限核对为 `600 root:root`；本次没有读取配置内容，也没有由 Agent 发送真实邮件。
- 用户已确认使用 QQ 邮箱后台新生成的授权码完成真实 SMTP 测试邮件发送，邮件发送结果通过；未回显或记录授权码内容。
- `mail.env` 必填项和 SMTP 配置校验通过，文件权限保持为 `600 root:root`。
- 手动执行 monitor service 通过：`Result=success`、`ExecMainStatus=0`，日志输出 `monitor_ok checked=backend,postgres,http,disk,backup,logs`。
- monitor timer 状态核对通过：`enabled`、`active`；QQ 邮箱邮件告警接入完成。

### 邮件告警接入完成状态

- 故障首次出现、持续故障冷却和恢复通知逻辑已在本地 mock 验证通过。
- ECS 使用 Python `3.6.8`，邮件脚本已调整为 Python 3.6 兼容写法；未替换系统 `/usr/bin/python3`。
- 真实 SMTP 邮件发送通过；后续仍需观察定时周期内的正常监控和必要时的实际故障通知行为。
