# 2026-09-18

## 完成内容

- 基于 2026-09-17 ECS `python:3.12-slim` 完整依赖预检的 34 个 wheel 和 SHA-256 清单，新增 backend 的 `requirements.lock`，锁定全部直接与传递依赖。
- backend Dockerfile 改为使用经 ECS 已有镜像核验的 Python 3.12 slim manifest digest，并仅通过阿里云 HTTPS PyPI 源、`--isolated`、`--only-binary=:all:` 和 `--require-hashes` 安装锁定依赖。
- 增加静态部署配置测试，防止构建回退为未锁定依赖、HTTP、`trusted-host` 或多索引配置。
- 根据 ICP 核准承诺，在前端首页底部增加 `粤ICP备2026139779号-1` 及工信部备案链接 `http://beian.miit.gov.cn`。
- 为正式域名 `tomato-agent-arena.me` 补充 Nginx TLS 1.2/1.3、会话安全和 HSTS 配置，并将部署样例的 `CORS_ORIGINS` 更新为正式 HTTPS 域名。
- 更新正式域名证书申请、自动续期和 HTTPS 验收文档；未执行 DNS、Certbot、ECS、防火墙或生产 Nginx 操作。

### 11:02–11:19 阶段 10：正式域名 HTTPS/TLS 部署

## 完成内容

- 经明确批准后，在 Alibaba Cloud Linux 3 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`，公网 IP `47.119.120.86`）完成 `tomato-agent-arena.me` 正式入口切换。
- 先创建并校验回滚备份目录 `/opt/multi-agent-arena/backups/https-20260918-110217`，保存切换前 Nginx、`.env.deploy` 和前端静态文件。
- DNS A 记录已生效；安装 Certbot `1.22.0`，通过 webroot 完成 Let’s Encrypt 证书申请，证书有效期至 2026-12-17。
- 部署正式 Nginx 配置，启用 TLS 1.2/1.3、HSTS、HTTP 到 HTTPS 跳转和证书续期 reload hook；公网 IP 灰度配置保留为回退入口。
- 发布包含 `粤ICP备2026139779号-1` 及工信部备案链接的前端构建，并将 ECS `CORS_ORIGINS` 更新为 `https://tomato-agent-arena.me`。
- 仅重建 backend 容器并确认 healthy；未执行数据库 migration、seed、restore、volume 删除或 PostgreSQL 重建。

## 执行命令

```bash
certbot certonly --webroot -w /var/www/certbot -d tomato-agent-arena.me
nginx -t
systemctl reload nginx
docker compose up -d --no-deps backend
certbot renew --dry-run --no-random-sleep-on-renew
```

## 验证结果

- HTTP `http://tomato-agent-arena.me/` 返回 `301` 并跳转到 HTTPS；HTTPS 首页返回 `200`。
- HTTPS `/health` 和 `/api/problems` 返回 `200`；正式 CORS 响应为 `https://tomato-agent-arena.me`。
- 首页实际加载的前端 JS 中包含 `粤ICP备2026139779号-1`；链接目标为 `http://beian.miit.gov.cn`。
- TLS 1.2 握手成功，TLS 1.1 被拒绝；证书 CN/SAN 为 `tomato-agent-arena.me`。
- Certbot 续期模拟最终返回 `status=0`；`certbot-renew.timer` 已 enabled/active，并显示下一次触发时间。
- Nginx、backend、PostgreSQL 均 healthy；backend 仅监听 `127.0.0.1:8000`，PostgreSQL 未映射公网端口，firewalld 保留 `ssh http https` 服务。
- 本地验证：部署配置测试 `6 passed`，前端测试 `8 passed`，前端生产构建和 `git diff --check` 通过。

## 遇到的问题

- Workbench 上传文件后首次独立 shell 未找到临时路径；通过远端文件探测和 SHA-256 校验确认文件已存在，未覆盖错误配置。
- 一次远端 shell 换行转义错误把 `.env.deploy` 写成了字面量 `\\n`，导致 Compose 配置校验失败；服务未因此停止，已从备份原子恢复，再用 `sed` 只替换 `CORS_ORIGINS` 并重建 backend。
- 上述错误的工具错误输出曾包含生产配置内容。相关 API Key 和 PostgreSQL 密码必须按已暴露处理并尽快轮换；本次未执行凭据轮换，不在日志中记录具体值。
- Certbot 首次 dry-run 使用默认随机等待导致 Workbench 命令超时；终止本次遗留进程、启用 timer 后，使用 `--no-random-sleep-on-renew` 重新验证成功。

## 解决方式

- 所有远端配置覆盖前先备份并校验 SHA-256；Nginx 只在 `nginx -t` 成功后 reload。
- `.env.deploy` 恢复后验证权限 `600`、文件哈希和 `docker compose config --quiet`，未触碰 PostgreSQL。
- 保留前端旧版本目录和 Nginx 备份作为应用级回滚点，不执行破坏性清理。

## 下一步

- 取得针对 Provider API Key 和 PostgreSQL 密码轮换的单独批准后，通过受保护方式完成轮换，并验证 backend；不得在命令行或日志中输出凭据。
- 继续完成正式白名单、Nginx 限流实测、额度/费用告警、异地备份和恢复演练；这些不因 HTTPS 上线而自动完成。

### 11:38 阶段 10 后续：生产凭据轮换完成

## 完成内容

- 已完成生产 Provider API Key 和 PostgreSQL 数据库凭据轮换，替换 HTTPS 部署期间按已暴露处理的旧凭据。
- 新凭据已通过受保护方式写入生产运行配置；具体值未写入代码、日志、命令或提交内容。

## 验证结果

- 用户确认凭据轮换已完成；本日志仅记录轮换状态，不记录任何敏感值。

## 下一步

- 继续保持凭据不出现在命令行、日志、前端构建产物和版本库中。
- 完成正式白名单、Nginx 限流实测、额度/费用告警、异地备份和恢复演练等剩余收口事项。

## 执行命令

```bash
# ECS：在 --network none、只读预检 wheel 挂载和 --rm 容器中执行
python -m pip install --dry-run --disable-pip-version-check \
  --no-index --find-links /wheels --require-hashes \
  -r /tmp/requirements.lock
```

## 验证结果

- 离线 `pip --dry-run` 成功解析 34 个包；未联网、未安装包、未修改生产服务或 ECS backend 构建上下文。
- 生成锁文件所用的 wheel 与 ECS `/tmp/arena-pip-preflight-20260917.sha256` 审计清单一一对应。

## 遇到的问题

- 初始锁文件导出辅助脚本依赖 `packaging`，而 `python:3.12-slim` 未预装该模块；失败发生在脚本元数据读取阶段，未进入 pip 解析。

## 解决方式

- 改用 Python 标准库 `zipfile` 读取 wheel 的 `.dist-info/METADATA`，在网络隔离容器中重新导出并完成 pip 哈希解析验证。

## 下一步

- 审查本次锁文件和 Dockerfile 变更；获得针对 backend-only ECS 构建的明确批准后，才可重试构建并验证 `EVALUATION_ENABLED=false` 下 `/api/evaluations` 返回 `503`。

### 00:31 阶段 5：已批准 backend-only ECS 构建失败并恢复

## 完成内容

- 经明确批准后，向 ECS backend 构建上下文同步了哈希锁定 Dockerfile、`requirements.lock` 及两份版本化 `judge_cases` JSON；四个输入文件在构建前均通过预期 SHA-256 校验。
- 构建命令 `docker compose up -d --build --no-deps backend` 在 backend 仍运行旧健康镜像时后台执行；未执行 migration、数据库写入、Nginx/controller 变更或其他服务重建。
- 构建失败后，已从权限 `700` 的回滚目录恢复 ECS 旧 Dockerfile，移除本次新增的 `requirements.lock`、正式/临时 `judge_cases` 文件及空目录；保留构建日志、回滚目录和下载暂存目录作为审计材料。

## 执行命令

```bash
cd /opt/multi-agent-arena/app
docker compose up -d --build --no-deps backend
```

## 验证结果

- 构建在拉取固定基镜像 manifest 的 metadata 阶段失败，退出码 `17`；尚未进入 `pip install` 或依赖哈希校验阶段。
- 失败根因：Docker daemon 配置的 registry mirror 对固定 manifest digest 的 HEAD 请求返回 `500 Internal Server Error`：

  ```text
  https://uk52zpri.mirror.aliyuncs.com/v2/library/python/manifests/sha256:78387b...
  ```

- 回滚后 ECS Dockerfile SHA-256 恢复为 `03f7f30c4a02e27343f162b7b545edd35c31e0f08fc3af236010de2c9017b55f`；运行 backend 仍为旧镜像 `sha256:67b6b0f56ac288ff10a18e5cc0f9fb5555998d41874dc00443e8dadc99021804` 且 healthy。
- `/health`、`/api/problems` 返回 `200`；旧 `/api/evaluations` 返回 `404`；controller inactive，Arena 候选容器数为 `0`。

## 遇到的问题

- 通过长 `workbench exec` 命令内嵌 Base64 同步时，Dockerfile 传输载荷出现 `--port` 拼写和末尾换行失真，two-sum JSON 也出现哈希不一致；SHA-256 门禁在原子落位前阻止了错误内容覆盖。

## 解决方式

- 对尚未落位的 Dockerfile 临时文件进行受限修正并重新验证 SHA-256；JSON 改用 `workbench upload` 经 OSS 中转按字节上传至权限 `700` 的临时目录，逐项哈希与 JSON 结构验证后再原子移动。
- 基镜像 manifest 的 registry mirror `500` 不属于 Python 包源或锁文件问题；未尝试绕过镜像 mirror、未修改 Docker daemon 配置，直接执行已批准恢复流程。

## 下一步

- backend 发布继续阻塞。需单独审查并验证固定 Python 基镜像在 ECS 当前 Docker registry mirror 下的可用方案（例如经确认可用的固定镜像引用或预构建镜像），再申请针对该方案的构建批准；不得在未解决镜像 manifest 获取失败前重试当前构建。

### 13:55 阶段 5：backend 发布、controller 私网接线及定时监控验证

## 完成内容

- 本条于 Asia/Shanghai 时间 2026-09-18 13:55 追加。远程操作由用户按逐步命令手动执行，以下 ECS 结果依据用户回传输出记录。
- 经 Daocloud 成功获取固定 Python 3.12 基础镜像 `m.daocloud.io/docker.io/library/python:3.12-slim@sha256:78387bc3881b8273120a12ebe6c1ab22b018ccc2c9adf565ae1ac9b536e184ea`，digest 与原 Dockerfile 锁定值一致，架构为 `linux/amd64`；未修改 Docker daemon。
- 完整 backend 构建包上传并通过 SHA-256 校验，在独立目录修改基础镜像入口、构建候选镜像，完成隔离检查及 backend-only 发布。
- 创建 `app_arena_sandbox` internal 网络，网段 `172.30.0.0/24`、网关 `172.30.0.1`；backend 保留 `app_arena_private` 并接入专用网络，私网地址为 `172.30.0.2`。
- 核对 controller 发布包、校验文件和已安装二进制一致，启动 controller，仅监听 `172.30.0.1:8001`。固定 Python 3.11 sandbox 镜像的 digest 和 `linux/amd64` 架构检查通过。
- 新版监控脚本通过哈希、Shell 语法及直接执行检查，备份并替换线上脚本；通过独立 systemd drop-in 和环境文件启用监控侧 sandbox 检查，随后取得定时执行成功证据。
- backend 始终保持 `EVALUATION_ENABLED=false`。监控进程中的同名开关为 `true`，仅启用 controller 健康及退出任务残留检查，不开启 backend 评测。
- 本轮未执行真实评测、Provider 调用、数据库 migration/seed 或 PostgreSQL 重建；隔离镜像检查使用自动清理的临时检查容器，不属于真实候选程序评测。

## 执行命令

以下为已由用户执行的关键命令摘要，不是完整重放脚本：

```bash
# 独立候选镜像构建；基础镜像入口仅在暂存 Dockerfile 中替换。
cd /tmp/arena-backend-build.kbOP5o/context
docker build --platform linux/amd64 --progress=plain \
  --tag arena-backend:stage5-kbOP5o .

# backend 发布及专用网络接线。
docker compose -p app \
  -f /opt/multi-agent-arena/app/compose.yaml \
  -f /opt/multi-agent-arena/backend-release.h0l5La/candidate.yaml \
  -f /opt/multi-agent-arena/backend-release.h0l5La/sandbox-network.yaml \
  up -d --no-deps --no-build --pull never \
  --wait --wait-timeout 120 backend

systemctl start multi-agent-arena-sandbox-controller.service
systemctl daemon-reload
```

## 验证结果

- 构建包 SHA-256：`bf3c710fb53aee862e45086d0154f18a673f36e2b52cc17c95d2326772c2e326`。包内包含锁文件、评测 API 和 Two Sum / Valid Parentheses 的版本化判题 JSON。
- 新 backend 镜像：`sha256:4fe7a89e8a50372e8693e688d267fb1479fb7ead4e37d15384590a28494cf329`，`linux/amd64`、运行用户 `app`、容器 healthy；哈希锁定依赖安装成功。
- 隔离镜像检查通过：非 root 用户、两份判题 JSON 可读取且语法有效、健康接口 `200`、评测关闭返回 `503 / Evaluation is disabled`。使用 `--network none`、只读根文件系统、资源限制、内存 SQLite 配置和 Mock Provider，未连接生产数据库。
- 发布及网络接线后：`/health` 返回正常内容、`/api/problems` 返回 `200`、`POST /api/evaluations` 返回 `503 / Evaluation is disabled`。
- controller 为 `active/running`；宿主机与 backend 内访问其 `/health` 均成功；检查时无 Arena 标签容器残留。
- controller 二进制 SHA-256：`dc8d2fca6ca11c9180cf5bc15a12de2797cc60445af7cf3b6b38ffa804a8ae07`。已证明与原发布包一致，未独立证明对应最新源码。
- 新版监控脚本 SHA-256：`c1df7800cdcd51d3185af7a7a68fb47d6c9e21511418bb5b7c4885ef1ad6701d`，安装权限 `root:root / 750`。原包内 monitor service 与本地源码哈希一致，但本轮未用该完整 service 替换线上 unit，而是新增 drop-in。
- 新版监控直接执行通过，输出 `monitor_ok checked=backend,postgres,http,sandbox,disk,backup,logs`；该直接检查使用脚本默认备份目录和阈值，未加载生产 `monitor.env`，未调用告警包装脚本。
- 监控 service 已加载 `/etc/multi-agent-arena/monitor.env` 和 `/etc/multi-agent-arena/monitor-sandbox.env`，drop-in 为 `/etc/systemd/system/multi-agent-arena-monitor.service.d/90-sandbox-check.conf`；timer 为 active/waiting，每 5 分钟执行。
- **13:53:32–13:53:33 CST 定时监控实际执行成功**：`Result=success`、`ExecMainStatus=0`，摘要包含 `sandbox`，确认新增检查已随 timer 生效。此前 13:48:25 的摘要不含 sandbox，是更新前的执行证据。
- 本轮未重新运行完整项目测试；以上镜像检查、HTTP 检查和定时监控结果不等同真实评测验收。

## 遇到的问题

- 原 registry mirror 获取固定 Python manifest 返回 `500`；ECS 旧构建上下文还缺少新版评测源码、锁文件和判题数据，仅改 Dockerfile 不足以发布。
- 线上旧监控脚本与当前源码不同，原监控 service 未接入 controller 检查；timer 使用单调时钟，`NextElapseUSecRealtime` 为空，不能据此判断调度失败。
- 当前线上运行仍依赖 Compose 覆盖文件，尚未固化到主 Compose；普通 `docker compose up` 可能切回旧镜像或移除私网接线。

## 解决方式

- 更换为显式 Daocloud registry 的同一 digest，先独立构建和隔离验证，再分步发布与接线；保留旧源码目录和旧镜像，不执行破坏性清理。
- 先验证新版监控脚本，再备份并原子替换；保留原告警包装脚本及凭据配置。新增环境文件只影响监控进程；drop-in 仅声明 controller 启动顺序，不自动拉起已停止的 controller。
- 等待下一轮 timer，以实际 journal 摘要确认 sandbox 检查生效，未为验证手动触发告警包装脚本；既有定时告警行为保留，未单独验证通知投递。
- 发布及配置备份目录：`/opt/multi-agent-arena/backend-release.h0l5La`，含主 Compose 和 `.env.deploy` 备份、`candidate.yaml`、`rollback.yaml`、`sandbox-network.yaml`；不在日志记录凭据值。
- 本次发布前旧镜像：`sha256:599dc32a9a76b0a70051a53fd03e87f05e3ca738d07fef4957e5ea8a9268948a`。以该实际核验值为本轮回滚依据，不沿用早期记录的旧镜像 ID。`rollback.yaml` 固定此镜像；主 Compose 加 `candidate.yaml` 可恢复到接线前已验证的新版 backend。回滚命令已准备，未实际执行回滚演练。
- 监控备份目录：`/opt/multi-agent-arena/backend-release.h0l5La/monitor-backup.eF7pUK`；新版监控暂存目录：`/opt/multi-agent-arena/backend-release.h0l5La/monitor-check.U4TWgY`。
- 构建包、上下文及构建日志保留在 `/tmp/arena-backend-build.kbOP5o`；原 controller 发布包位于 `/tmp/arena-release-20260917/arena-release-20260917.tar.gz`。

## 下一步

- 完成部署配置固化，明确日常 Compose 管理入口，避免遗漏候选镜像和专用网络覆盖文件；本地 Dockerfile 仍为原 Docker Hub 固定 digest 引用，下载入口变化仅在 ECS 暂存构建副本中。
- 完成 controller 开机启动与 Docker 网络就绪顺序验证；最近核验的自启状态仍为 `disabled`，当前仅手动启动。
- 核对评测相关 Nginx 配置、入口限制和相应验收；本轮未更新 Nginx。
- 完善关闭路径和回滚证据后，单独取得真实评测、Provider 调用及候选容器操作许可，再执行真实评测验收。
- 主机总内存约 1870 MiB，检查时 available 约 1271 MiB；该快照不构成评测容量承诺。本次完成的是发布、私网连通和监控检查，不代表阶段 5 全部验收完成或强安全隔离成立。

### 15:04 阶段 5：配置固化、controller 自启及 Nginx 关闭路径验收

## 完成内容

- 主 Compose 已原子替换并固化到 `/opt/multi-agent-arena/app/compose.yaml`；候选配置移除 backend `build`，固定已验证 backend 镜像 `sha256:4fe7a89e8a50372e8693e688d267fb1479fb7ead4e37d15384590a28494cf329`，设置 `pull_policy=never`，保留评测关闭和 `arena_sandbox` 专用网络。
- controller 已启用 systemd 开机自启；unit 状态为 `enabled`，当前 `active/running`，仅监听 `172.30.0.1:8001`。
- 线上 Nginx 生效配置已完成只读核对，评测路由保留独立限流和超时配置；本轮无需 reload Nginx。

## 验证结果

- 主 Compose 不带覆盖文件解析通过；backend healthy，连接 `app_arena_private` 和 `app_arena_sandbox`，PostgreSQL 仍仅连接 `app_arena_private`。
- 主 Compose 配置摘要确认：`EVALUATION_ENABLED=false`、`SANDBOX_CONTROLLER_URL=http://172.30.0.1:8001`、sandbox 网段为 `172.30.0.0/24` 且 `internal=true`。
- Nginx `nginx -t` 通过；生效配置包含 `evaluation_by_ip`、评测路由 `1r/m` 限流、`429` 限流响应和 `220s` 读取超时。
- 监听边界符合预期：backend 仅监听 `127.0.0.1:8000`，controller 仅监听 `172.30.0.1:8001`，宿主机未监听 PostgreSQL `5432`。
- 公网 HTTPS `/health` 返回 `200`，`/api/problems` 返回 `200`，评测关闭路径返回 `503 / Evaluation is disabled`。
- Nginx 评测路由无效请求返回 `422`，立即重复请求返回 `429`；验证前后 Arena owner 标签容器集合不变，无新建或残留临时容器。
- controller 开机自启验证未重启 ECS，不能替代真实重启后的恢复验收。

## 保留限制与下一步

- 尚未执行真实评测、Provider 调用或候选 Arena 容器创建/删除；不得将本次关闭路径、Nginx 限流和 controller 健康检查视为真实评测验收。
- 尚未进行 ECS 重启后的 controller、Docker 网络和 backend 恢复验证；当前主机内存约 1.87 GiB，不能据此承诺评测容量。
- 本地 Dockerfile 仍保留原始 Python 固定 digest 引用；本次 ECS 构建仅在暂存副本中使用显式 Daocloud 同 digest 入口，日常 Compose 固定已构建镜像，不再从 ECS 旧源码目录构建。
- 真实评测仍需另行取得针对 Provider 调用、`problem_id=2` 请求和最多 9 个 Arena 临时容器生命周期的明确批准。

### 17:18 Task 2 本地部署工件：controller 启动前 Docker 网络门禁

## 完成内容

- 新增 `/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/deploy/systemd/check-sandbox-network.sh`，在 controller 启动前校验 `app_arena_sandbox` 存在、driver 为 `bridge`、`internal=true`、网段为 `172.30.0.0/24` 且网关为 `172.30.0.1`。
- 更新 `/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/deploy/systemd/multi-agent-arena-sandbox-controller.service`，增加 `ExecStartPre` 和 `SANDBOX_DOCKER_NETWORK`；网络不满足条件时 controller fail closed，不改绑公网或其他地址。
- 更新部署静态测试，覆盖 systemd 前置检查和网络边界常量。

## 执行命令

```bash
sh -n deploy/systemd/check-sandbox-network.sh
python3 -m unittest tests/test_deployment_config.py
cd sandbox && ../.tools/go-1.24.0/bin/go test ./cmd/controller ./internal/executor ./internal/httpapi
deploy/systemd/check-sandbox-network.sh
```

## 验证结果

- Shell 语法检查通过；脚本权限为 `750`。
- 部署静态测试 `7 passed`。
- controller、executor、httpapi Go 单元测试全部通过。
- 本地直接执行网络检查返回非零并报告 `network unavailable`，因本地没有 `app_arena_sandbox` 网络，符合 fail-closed 预期；未创建本地 Docker 网络以伪造 ECS 验收。
- 未执行 ECS 安装、`systemctl daemon-reload`、controller 重启、ECS 重启、数据库操作、Provider 调用或真实评测。

## 下一步

- 取得针对 ECS 安装/替换该检查脚本、执行 `daemon-reload` 和 controller 恢复验证的明确批准后，按备份、校验、原子替换流程落地。
- Task 3 生产 Provider 配置、Task 4 管理 IP 白名单和 Task 5 真实 Provider 连通性仍需分别明确目标参数、影响范围和批准；在此之前保持 `EVALUATION_ENABLED=false`。

### 19:29 Task 1：ECS 生产只读基线与收口门禁

## 完成内容

- 通过 Workbench 对 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）执行只读基线检查；未写入生产配置、未执行 systemd 操作、未重启、未调用 Provider、未执行数据库操作或真实评测。
- 确认 backend 镜像为 `sha256:4fe7a89e8a50372e8693e688d267fb1479fb7ead4e37d15384590a28494cf329`，backend/PostgreSQL healthy，controller `enabled` 且 `active`，sandbox 网络为 internal `bridge`、网段 `172.30.0.0/24`、网关 `172.30.0.1`，Arena owner 标签容器数为 `0`。
- 确认 controller `/health`、backend `/health`、`/api/problems` 正常，`POST /api/evaluations` 对合法 `problem_id=2` 请求返回 `503`；Nginx 语法检查通过，监控 timer 为 `active/waiting` 且最近结果为 `success`。
- 记录当前 Compose SHA-256、controller 二进制 SHA-256、监控脚本 SHA-256，以及 candidate、rollback、sandbox-network 回滚材料 SHA-256；回滚 YAML 固定旧 backend 镜像且未检测到数据库操作关键字。

## 验证结果与阻塞项

- 生产 `.env.deploy` 未显式设置 `EVALUATION_ENABLED=false`，当前依赖应用默认值关闭；虽然关闭路径已返回 `503`，但不满足计划要求的显式、可审计关闭配置。
- 生产 Provider 配置摘要为 OpenAI Compatible、API Key 和模型非空、base URL 为 HTTPS；未读取或记录具体 Provider、URL、模型或凭据值，也未发起 Provider 调用。
- ECS controller unit 当前没有 `ExecStartPre` 网络门禁或 drop-in；本地已验证的 `check-sandbox-network` 尚未安装，因此 ECS 重启恢复顺序未完成验收。
- Nginx 评测精确路由保留 `1r/m`、`429` 和 `220s`，但没有 IP 白名单、`deny all` 或认证；真实评测接口不得开放。

## 下一步

- 分别取得针对以下生产写入的明确批准：显式写入 `EVALUATION_ENABLED=false` 并最小重建 backend；安装 controller 网络检查、替换/新增受控 systemd 配置并执行 `daemon-reload` 与 controller 重启；配置经确认的管理出口 IP 白名单并在 `nginx -t` 后 reload。
- 在未完成以上门禁且未取得真实 Provider 调用、真实评测和 ECS 重启的单独批准前，保持评测关闭状态。

### 20:08 Task 1+2：生产显式关闭评测与 controller 网络启动门禁

## 完成内容

- 经明确批准，在 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）创建权限 `700` 的回滚目录 `/opt/multi-agent-arena/backend-release.h0l5La/task1-2-20260918-195853`，备份原 `.env.deploy` 和 controller systemd unit。
- 通过受限暂存目录、SHA-256 校验和原子替换，向生产 `.env.deploy` 显式写入 `EVALUATION_ENABLED=false`；配置文件保持 `root:root / 600`，未输出或修改 Provider/API Key/数据库密码。
- 安装本地已验证的 `/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/deploy/systemd/check-sandbox-network.sh` 到 ECS `/usr/local/libexec/multi-agent-arena/check-sandbox-network`，权限为 `root:arena-sandbox / 750`。
- 更新 ECS `/etc/systemd/system/multi-agent-arena-sandbox-controller.service`，加入 `SANDBOX_DOCKER_NETWORK=app_arena_sandbox` 和 `ExecStartPre` 网络门禁；执行 `systemctl daemon-reload`，仅重建 backend 和重启 controller，未重启 ECS。

## 执行命令

```bash
docker compose -f /opt/multi-agent-arena/app/compose.yaml up -d --no-deps --pull never backend
systemctl daemon-reload
systemctl restart multi-agent-arena-sandbox-controller.service
```

## 验证结果

- 最终生产脚本 SHA-256：`20e590dafaa1d968dda0b829f4897918693fee8f997a2fea34f1b6bae09f7dd1`；systemd unit SHA-256：`07dd78a0c45f576cb99b2075fb0a104c891c73be2d27b6c4a929b882a4b7183a`。
- 以 `arena-sandbox` 用户在 ECS 真实 Docker 网络执行门禁成功：`app_arena_sandbox` 为 `bridge`、`internal=true`、网段 `172.30.0.0/24`、网关 `172.30.0.1`。
- backend/PostgreSQL healthy；controller `enabled/active`；controller `/health=200`；backend `/health=200`；`/api/problems=200`；合法 `problem_id=2` 的评测请求返回 `503`。
- `EVALUATION_ENABLED=false` 已显式存在；Arena owner 标签容器数为 `0`；监控 timer `success,active,waiting`。
- 监听边界符合预期：controller 仅监听 `172.30.0.1:8001`，无其他公网 `8001`；backend 仅监听 `127.0.0.1:8000`；宿主机无公网 `5432`。
- `systemd-analyze verify` 通过；回滚目录权限为 `700`，原环境和 unit 备份存在。

## 遇到的问题与解决方式

- 第一次安装尝试因检查脚本 Docker Go template 使用 `%s` 格式化布尔值，产生 `%!s(bool=true)` 并误判网络；自动回滚恢复原 `.env.deploy`、原 unit、backend 和 controller，回滚后健康检查及 `503` 关闭路径均正常。
- 修正为分别读取 Docker `Driver`、`Internal`、`Name`、`Subnet` 和 `Gateway` 字段；同时将脚本安装权限调整为 `root:arena-sandbox / 750`，确保 systemd 服务用户可执行并通过 Docker supplementary group 访问 socket。
- 暂存验证初次受目录 `700 root:root` 阻挡，未触碰生效文件；调整暂存目录为可遍历权限后，真实 ECS 用户态验证成功，再执行最终落位。

## 未完成事项与下一步

- 未执行 ECS 整机重启，因此 controller/Docker 网络/backend 的真实重启恢复验收仍未完成。
- Nginx 评测精确路由仍无管理 IP 白名单或认证，仅保留限流和超时；未执行 Nginx reload。
- 未调用真实 Provider，未执行真实评测，未创建/删除 Arena 临时容器，未执行数据库 migration/seed/restore 或 PostgreSQL 重建。
- Task 3 Provider 真实连通性、Task 4 管理出口白名单、Task 5 真实 Provider 调用和 Task 6 真实评测仍需分别取得具体范围批准。

### 20:27 Task 4：评测入口管理出口 IP 白名单

## 完成内容

- 根据用户提供的当前公网出口 IPv4 `103.151.172.86`，在本地正式域名和灰度 Nginx 模板的精确 `location = /api/evaluations` 中加入临时白名单 `allow 103.151.172.86/32;` 和 `deny all;`。
- 保留评测路由原有 `1r/m` 限流、`429` 响应、`220s` 读取超时及代理头；未给 `/api/` 通配路由增加绕过规则。
- 在 ECS `/etc/nginx/conf.d/multi-agent-arena.conf` 原子替换正式配置；替换前备份至权限 `700` 的 `/opt/multi-agent-arena/backend-release.h0l5La/nginx-allowlist-20260918-202329/`，并在 `nginx -t` 成功后 reload。

## 执行命令

```bash
nginx -t
systemctl reload nginx
```

## 验证结果

- 本地部署静态测试 `8 passed`，`git diff --check` 通过。
- ECS 暂存配置 SHA-256 与本地正式配置一致：`d6647cfcc875a5e2cb337546f88a4cc80a845fe27825b00d060e82707d510e8b`。
- reload 后 `nginx -t` 通过，生效配置包含白名单和 `deny all`。
- ECS 本机通过 HTTPS/SNI 访问评测入口返回 `403`，证明未授权来源被 Nginx 拒绝。
- 公网 `/health=200`、`/api/problems=200`；backend 直连仍保持评测关闭；Arena owner 标签容器数为 `0`；backend/PostgreSQL/controller 和监控状态未受影响。
- HTTP 入口验证返回 `301`，随后补充 HTTPS 验证；一次远端验证命令因嵌套引号错误未执行请求，不影响线上配置。

## 限制与下一步

- `103.151.172.86` 是当前用户提供的校园网出口候选地址，可能是共享或动态 NAT 出口，适合作为短期灰度白名单，不应视为长期唯一管理身份。
- 尚未从用户实际公网出口执行授权正向请求；需要在 Windows 主机确认公网出口仍为 `103.151.172.86` 后，请求 `POST https://tomato-agent-arena.me/api/evaluations`，评测关闭时预期返回 `503`。
- 尚未执行真实 Provider 调用、真实评测、ECS 整机重启或数据库操作。

### 20:37 Task 4 后续：将白名单替换为实际公网出口

## 完成内容

- 用户从 Windows 主机通过两个独立公网查询服务确认当前公网出口为 `36.248.198.60`；此前配置的 `103.151.172.86/32` 与当前出口不一致。
- 经明确批准，将本地正式/灰度 Nginx 模板及生产正式配置中的白名单从 `103.151.172.86/32` 替换为 `36.248.198.60/32`，不保留旧地址。
- 在 ECS 创建权限 `700` 的备份目录 `/opt/multi-agent-arena/backend-release.h0l5La/nginx-allowlist-3624819860-20260918/`，备份替换前正式配置；通过暂存文件 SHA-256 校验后原子替换并 reload Nginx。

## 验证结果

- 本地部署静态测试 `8 passed`，相关 `git diff --check` 通过。
- 新正式配置 SHA-256：`0d2761b263bd795cc0fbef7b9c37aceb9b8abb3c2359037c1773c12590770298`。
- reload 后 `nginx -t` 通过；生效配置包含 `allow 36.248.198.60/32;` 和 `deny all;`，旧 `103.151.172.86/32` 已不存在。
- ECS 本机 HTTPS/SNI 未授权请求返回 `403`；公网 `/health=200`、`/api/problems=200`；backend 直连评测关闭路径返回 `503`。
- backend/PostgreSQL healthy，controller active，监控 timer `success,active,waiting`，Arena owner 标签容器数为 `0`。

## 限制与下一步

- `36.248.198.60` 可能是校园网共享或动态 NAT 出口，仅作为当前短期灰度白名单；网络切换后需要重新确认出口并单独更新配置。
- 尚未从用户 Windows 公网出口发起最终授权正向请求；评测保持关闭时，授权请求预期返回 `503`。
- 未调用真实 Provider、未执行真实评测、未重启 ECS、未执行数据库写操作。

### 20:52 Task 2：ECS 整机重启恢复验收

## 完成内容

- 经用户明确批准，在 Alibaba Cloud Linux 3 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）执行一次整机重启，验证 controller、Docker 网络、backend、PostgreSQL 和监控的开机恢复顺序。
- 重启前完成脱敏基线：backend/PostgreSQL healthy，controller 和监控 timer 为 `enabled/active`，`EVALUATION_ENABLED=false`，评测关闭路径返回 `503`，Arena owner 标签容器数为 `0`，Nginx 配置测试通过。
- 本次仅执行整机重启和只读验收；未执行数据库 migration/seed/restore、Provider 调用、`EVALUATION_ENABLED=true`、真实评测、Arena 临时容器创建/删除、Nginx 配置变更或 Docker daemon/防火墙变更。

## 执行命令

```bash
# ECS：已批准的整机重启；重启前后均未执行数据库或评测操作
nohup sh -c "sleep 2; /sbin/reboot" >/dev/null 2>&1 &
```

## 验证结果

- ECS 于 `20:47:37 CST` 完成新一轮启动；controller 于 `20:47:54 CST` 启动并返回 `Result=success`、`ExecMainStatus=0`，启动日志包含 `sandbox_network_ok`。
- `app_arena_sandbox` 仍为 `bridge`、`internal=true`、网段 `172.30.0.0/24`、网关 `172.30.0.1`；启动前网络门禁执行成功。
- controller 与 monitor timer 均为 `enabled/active`；controller `/health=200`，backend/PostgreSQL 为 `running/healthy`，监控最近一次执行 `Result=success`、`ExecMainStatus=0`，摘要包含 `sandbox`。
- backend `/health=200`、`/api/problems=200`、直连 `POST /api/evaluations=503`；正式 HTTPS `/health=200`、`/api/problems=200`。从 ECS 发起的评测入口请求返回 `403`，符合当前管理出口白名单保护；未触发评测。
- 监听边界保持符合预期：backend 仅监听 `127.0.0.1:8000`，controller 仅监听 `172.30.0.1:8001`，未发现 PostgreSQL `5432` 公网监听；Arena owner 标签容器数为 `0`；`nginx -t` 通过。

## 遇到的问题

- 重启后第一次合并验收命令因 Workbench 返回未分类连接错误未取得输出；随后一次最小时间读取命令返回 `connection closed`。这两次均未改变远端状态。

## 解决方式

- 等待 ECS 和 Workbench session 恢复后，将验收拆分为多个短只读命令重试；所有核心服务、启动门禁、HTTP、监听、监控和容器残留检查均成功。

## 下一步

- Task 2 已完成。继续前仍保持 `EVALUATION_ENABLED=false`；Task 3 Provider 配置/真实连通性、Task 5 受控 Provider 调用、Task 6 单次真实评测和 Task 7 评测后关闭审计仍需分别取得具体批准。
- 当前不报告“真实 LLM 评测功能完整生产上线”；仍需完成 Provider 费用/配额、异常路径和真实评测链路验收。

### 21:53 Task 5：受控真实 Provider 连通性验证

## 完成内容

- 经用户明确批准，在 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）现有 backend 容器内最多执行 1 次真实 OpenAI Compatible Provider 调用；未重新写入 Provider 配置。
- 调用前确认 backend/PostgreSQL healthy、controller `/health=200`、`EVALUATION_ENABLED=false`、评测关闭路径 `503`，Arena owner 标签容器数为 `0`。
- 使用不包含生产密钥、隐藏判题用例或完整题库数据的最小固定输入，只验证 Agent 生成的 `AgentEvaluationOutput` 结构；未调用 `/api/evaluations`，未创建或删除 Arena 临时容器。

## 执行命令

```bash
# ECS：backend 容器内受控执行一次 Provider/Agent 结构化输出检查
docker compose -p app -f /opt/multi-agent-arena/app/compose.yaml exec -T backend python -

# 本地 Provider、Agent 和评测错误契约回归
backend/.venv/bin/python -m pytest -q backend/tests/test_openai_compatible_provider.py backend/tests/test_evaluations.py
```

## 验证结果

- 唯一一次真实 Provider 调用成功：耗时约 `6809ms`，响应摘要大小 `228 bytes`，代码大小 `112 bytes`，说明大小 `59 bytes`。
- 返回内容通过 `AgentEvaluationOutput` 校验：`language=python`、代码非空、无 Markdown 围栏、无额外字段。
- 调用后 backend/PostgreSQL 仍为 `running/healthy`；backend `/health=200`，controller `/health=200`，评测关闭路径仍为 `503`，Arena owner 标签容器数仍为 `0`。
- controller 在本次调用前后检查窗口内未出现 `/execute` 请求痕迹；monitor timer 为 `active`，最近结果为 `success/0`。
- backend 最近日志敏感字段标记数为 `0`，Prompt/原始模型响应标记数为 `0`；未输出 API Key、完整 Prompt、完整模型输出或完整日志。
- 本地目标测试：`34 passed`，`221 warnings`，无失败。

## 未完成事项与限制

- 当前 `OpenAICompatibleProvider` 只提取上游 `choices[0].message.content`，没有保存 `usage` 中的 token 信息；本次无法从应用侧提供 token/费用证据，也未再次调用 Provider 补采集。
- 因 token/费用可追踪性尚未闭环，Task 5 的真实调用和结构化输出部分完成，但不标记 Task 5 整体完全通过。
- 本次未设置 `EVALUATION_ENABLED=true`，未调用真实评测接口，未创建/删除 Arena 临时容器，未执行数据库操作。

## 下一步

- 从 Provider 控制台或已批准的费用记录渠道核对本次单次调用的 token/费用；在此之前不进入 Task 6。
- Task 6 仍需另行取得具体批准，批准范围必须包括临时设置 `EVALUATION_ENABLED=true`、`problem_id=2`、Provider 调用以及最多 9 个 Arena 临时容器的创建/删除。

### 22:17 Task 6/7：单次真实评测灰度与关闭恢复

## 完成内容

- 经用户明确批准，并在已知 Task 5 token/费用追踪尚未闭环的例外下，针对 ECS `i-wz95igsjwjrfzpckjip5` 发起 1 次 `problem_id=2` 真实评测请求。
- 发现生产 Compose 的 backend 同时以 `environment` 硬编码 `EVALUATION_ENABLED: 'false'`，其优先级高于 `.env.deploy`；两次启用尝试均在实际请求前 fail-closed 并自动恢复，未调用 Provider、未创建 Arena 容器。
- 经针对该 Compose 覆盖项的单独批准，备份 Compose 和 `.env.deploy` 到权限 `700` 的 `/opt/multi-agent-arena/backend-release.h0l5La/task6-compose-20260918-221656/`，临时将两处开关改为 `true`，仅使用主 Compose `--force-recreate` 替换 backend。
- 真实请求完成后，自动恢复原 Compose 和 `.env.deploy`，再次仅强制重建 backend；未修改 PostgreSQL、Nginx、controller、Docker daemon、防火墙或数据库数据。

## 验证结果

- 启用后的 backend 运行时 `evaluation_enabled=true`；唯一一次真实请求返回 `HTTP 200`，耗时 `20` 秒，响应 `227 bytes`。
- `problem_id=2` 实际解析为 `valid-parentheses`，Judge 返回 `AC`：`case_version=v1`、`12/12` 已执行和通过、无失败用例、响应无额外字段且包含摘要。
- Arena 容器峰值为 `1`，请求结束后 owner 标签容器数为 `0`；Docker events 记录 `12` 次创建和 `12` 次销毁，controller 未发现 fatal/panic/OOM/error 标记。
- 自动关闭恢复后：Compose SHA-256 恢复为 `73f574af561493c6d152ac0346b94441f94831524693642f03500904d5309351`，硬编码和 `.env.deploy` 均为 `EVALUATION_ENABLED=false`，backend 运行时开关为 `false`。
- 恢复后 backend/PostgreSQL 为 `running/healthy`，`/health=200`、`/api/problems=200`、`POST /api/evaluations=503`、controller `/health=200`；controller 为 `active`，monitor timer 为 `active` 且最近结果 `success/0`，监听仍仅为 backend `127.0.0.1:8000`、controller `172.30.0.1:8001`。
- backend、Nginx 日志敏感字段和原始请求/响应标记检查均为 `0`；未输出 API Key、完整 Prompt、完整候选代码或完整日志。

## 范围偏差与限制

- 批准范围和计划误将 `problem_id=2` 视为 Two Sum v1 的最多 9 次容器生命周期；实际 `problem_id=2` 是 Valid Parentheses v1，有 12 个用例，导致顺序创建/删除 12 次临时容器，超过批准的 9 次上限。虽然峰值仅为 1 且全部清理，本次不能标记为无偏差的 Task 6 完整验收。
- 本次未从 Provider 侧取得 token/费用记录；Task 5 和 Task 6 的费用追踪仍未闭环。
- 未逐任务采集并审计所有容器的只读根文件系统、tmpfs、非 root、capability drop、资源限制和 5 秒墙钟参数；本次仅保留受限生命周期摘要。

## 下一步

- 更正计划中的题目 ID 与用例数量映射，并在任何后续真实评测批准中按实际题目明确容器生命周期上限。
- 从 Provider 控制台或受控费用记录渠道核对两次真实 Provider 调用（Task 5 和本次评测）的 token/费用；补齐容器运行参数审计后，才能重新评估 Task 5/6 是否完整通过。
- 保持 `EVALUATION_ENABLED=false`；当前仍不得报告真实 LLM 评测功能完整生产上线或长期开放评测入口。

### 22:51 Task 5–7 后续：费用追踪延后

## 完成内容

- 用户明确确认当前无需考虑 Provider token/费用问题；实施计划已将 token/费用观测和费用上限从当前灰度收口门禁调整为后续运营事项。
- Task 5 以已记录的单次调用、响应时间、响应摘要和结构化输出为当前通过依据；Task 6/7 不再因未取得 Provider 侧费用记录而保持未完成。

## 验证结果

- 未调用 Provider、未发起评测、未创建或删除 Arena 容器、未修改 ECS 配置或服务。
- 已保留历史真实调用的次数和耗时证据；当前 `OpenAICompatibleProvider` 仍未采集上游 `usage`，这是延后的可观测性能力，不作为当前阻塞项。

## 保留限制与下一步

- Task 6 仍不能标记为无偏差完整通过：`problem_id=2` 实际产生 12 次容器生命周期，超过当时批准的 9 次上限；且未逐容器保留只读根文件系统、tmpfs、非 root、capability、资源限制和墙钟参数的运行时审计证据。
- 继续保持 `EVALUATION_ENABLED=false`。在更正题目 ID/用例映射、补齐运行参数审计及完成 Task 8 的非费用门禁前，不得报告长期开放评测或真实 LLM 评测功能完整生产上线。

### 23:21 Task 6 收口：本地脱敏容器运行参数审计

## 完成内容

- 按用户选择实现方案 1：controller 新增独立 JSONL 运行参数审计，目标生产路径为 `/var/lib/multi-agent-arena-sandbox-audit/tasks.jsonl`。
- 审计记录仅包含固定镜像 digest、网络模式、只读根文件系统、tmpfs、UID/GID、CPU/内存/swap/PID 限制、capability、`no-new-privileges`、5 秒墙钟、退出结果、OOM 状态、耗时和清理结果。
- 明确禁止写入候选代码、stdin、stdout、stderr、Provider 内容、API Key、数据库密码和完整环境变量。
- controller systemd 模板增加 `StateDirectory=multi-agent-arena-sandbox-audit`、`StateDirectoryMode=0750` 和审计文件环境变量；新增 logrotate 配置，每日轮转、压缩、最多保留 3 个轮转文件并设置 `maxage 3`。

## 验证结果

- TDD RED 阶段按预期失败：审计接口和三天轮转配置不存在时，Go/部署测试失败。
- TDD GREEN 阶段通过：sandbox 全量 Go 测试通过，`go vet ./...` 通过；部署静态测试 `9 passed`；`git diff --check` 通过。
- 脱敏审计单元测试确认记录不包含候选代码、stdin、stdout、stderr 或凭据字段，并包含固定运行参数和清理状态。

## 限制与下一步

- 本次只修改本地源码、测试和部署模板，未向 ECS 安装二进制、systemd 配置或 logrotate 配置，未执行 daemon-reload、controller 重启、Provider 调用或真实评测。
- 下一步需取得针对 ECS 审计功能安装、备份、原子替换、systemd reload 及关闭状态验证的明确批准；安装后再进行一次单独的受控真实评测，获取逐任务 JSONL 审计证据。评测仍需按实际 `problem_id` 和用例数审批，当前保持 `EVALUATION_ENABLED=false`。
