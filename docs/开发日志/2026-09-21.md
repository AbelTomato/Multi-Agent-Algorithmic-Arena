# 2026-09-21

## 2026-09-21 11:12（Asia/Shanghai）

## 完成内容

- 继续执行阶段 D 纯 Submission Evaluation 验收，修正 Evaluation 摘要的 `case_version` 持久化语义：始终使用服务端从题目读取的当前激活用例版本，不再信任 Judge 返回的版本字段。
- 新增回归测试，验证题目激活版本为 `v2` 而 Fake Judge 返回 `v1` 时，评测输入和持久化 Evaluation 仍绑定 `v2`。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m compileall -q app tests
LLM_PROVIDER=mock ARENA_SANDBOX_INTEGRATION=0 .venv/bin/python -m pytest -q tests/test_auth_subjects.py tests/test_submissions.py tests/test_candidate_generation.py tests/test_submission_evaluations.py tests/test_submission_api.py tests/test_stage_d_migration.py tests/test_problem_model.py
ARENA_SANDBOX_INTEGRATION=0 .venv/bin/python -m pytest -q tests/integration/test_evaluation_flow.py tests/integration/test_submission_evaluation_flow.py
```

## 验证结果

- TDD RED 阶段按预期失败：新增测试观察到 `v1` 被错误持久化为 Evaluation 版本。
- 修复后阶段 D 目标测试 **30 passed**。
- 集成测试 **2 passed、1 skipped**；跳过项为真实 Go Sandbox 控制器测试，因 `ARENA_SANDBOX_INTEGRATION=0`。
- Python `compileall` 通过。
- 未执行持久 PostgreSQL migration、seed、账户写入、部署、真实模型请求或真实 Docker 外部操作。

## 遇到的问题

- `SubmissionEvaluationService` 已按服务端 `active_case_version` 加载用例，但持久化时错误使用了 Judge 返回的 `result.case_version`。

## 解决方式

- 将 Evaluation 的 `case_version` 改为服务端选择的局部变量 `case_version`，并增加错误 Judge 版本返回的回归测试。

## 下一步

- 阶段 D 仍待在单独授权且具备真实控制器的环境补充 Python/C++ 等价 Checker 集成。
- 阶段 D 仍待补充服务重启/重复查询不改变 Submission 与 Evaluation 摘要的独立覆盖。
- 持久数据库 migration 仍须单独审批后执行。

## 2026-09-21 11:48（Asia/Shanghai）

## 完成内容

- 继续推进阶段 D 任务 8，在隔离 SQLite 测试中补充“服务重启/重复查询”回归覆盖：完成评测后关闭原 Session，以新 Session 重新读取同一 Submission 与 Evaluation，确认不可变源码快照、哈希、语言、Runtime、用例版本、状态摘要及关联保持不变。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m pytest -q tests/test_submission_evaluations.py
.venv/bin/python -m compileall -q app tests
```

## 验证结果

- `tests/test_submission_evaluations.py`：**4 passed**。
- Python `compileall` 通过。
- 测试仅创建 pytest 临时 SQLite 数据库；未执行持久 PostgreSQL migration、seed、账户写入、部署、真实模型请求或真实 Docker 外部操作。

## 遇到的问题

- 阶段 D 剩余的 Python/C++ 等价 Checker 集成需要真实 Go 控制器，并会创建和清理 Docker 容器；当前未获得该外部操作的单独授权。

## 下一步

- 获得针对真实 Docker 集成测试的明确授权并确认控制器环境后，补充 Python/C++ 等价候选的同题 I/O 与 Checker 一致性验证。
- 持久数据库 migration 仍须单独审批后执行。

## 2026-09-21 15:05（Asia/Shanghai）

## 完成内容

- 完成阶段 D 任务 8 的真实评测验证：将旧匿名 `POST /api/evaluations` 直评测测试迁移为 `POST /api/submissions` → `POST /api/submissions/{submission_id}/evaluations`，并将 busy-controller 测试请求迁移到冻结的 `execution-api-v2` 契约。
- 新增隔离 SQLite 的真实 Submission→Evaluation 集成测试：同一 Two Sum 题目的等价 Python/C++ 正确候选均得到 `AC`，等价错误候选均得到 `WA`；两种语言使用服务端固定 Runtime、相同 JSON I/O 和同一 Checker。
- 真实 Go/Docker Controller 验证覆盖控制器生命周期、C++ 编译、资源边界、重启恢复、Backend Judge 链路和 Python/C++ 等价 Checker 行为。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m pytest -q tests/integration/test_evaluation_flow.py tests/integration/test_submission_evaluation_flow.py tests/integration/test_judge_sandbox.py
.venv/bin/python -m compileall -q app tests

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
ARENA_SANDBOX_INTEGRATION=1 /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go test ./tests/integration -v -count=1

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
ARENA_SANDBOX_INTEGRATION=1 LLM_PROVIDER=mock .venv/bin/python -m pytest -q tests/integration/test_evaluation_flow.py tests/integration/test_submission_evaluation_flow.py tests/integration/test_judge_sandbox.py
```

## 验证结果

- 默认 Backend 集成测试：**2 passed、12 skipped**；跳过项要求显式启用真实 Controller。
- Python `compileall`：通过。
- Go 真实 Docker 集成套件重跑：**PASS**，`44.561s`，全部场景通过。
- Backend → Judge → Go Controller 真实集成：**14 passed、0 failed**，`110.17s`。
- 真实测试结束后，Arena owner 标签容器和 volume 均为零；临时 Controller 已停止，`127.0.0.1:8001` 已释放。

## 遇到的问题

- 首次真实 Backend 集成发现 2 个测试契约问题：一个测试仍断言已移除的匿名创建入口返回 200；busy-controller 测试仍发送旧协议字段，导致第一请求在进入执行槽前被拒绝。
- 首次 Go 真实集成中，Controller 重启测试因临时 Controller 占用 `127.0.0.1:8001` 失败；停止外部临时 Controller 后完整重跑通过。

## 解决方式

- 按阶段 D Spec 将 API 测试迁移到 Submission 创建和 Submission Evaluation 路径；旧入口保留 405/历史只读断言。
- 按 `execution-api-v2` 冻结契约补齐 `api_version`、`runtime_id`、`source`、`stdin_input` 和 `io_protocol` 字段。
- 分离 Go Controller 生命周期测试与 Backend 真实评测测试的 Controller 生命周期，避免端口占用造成环境冲突。

## 下一步

- 阶段 D 真实 Python/C++ Checker 集成项已完成；后续如扩展题目或 Checker，需为新增语义补充同等级真实集成覆盖。
- 持久 PostgreSQL migration、seed、账户/权限写入仍未执行，须单独审批后进行。

## 2026-09-21 20:10（Asia/Shanghai）

## 完成内容

- 完成阶段 E 任务 4：实现基础设施暂停与恢复，暂停时按 UTC 冻结剩余作答秒数，owner 恢复时重建 `solving_deadline`。
- 实现 `LOCKED` 状态下的 Evaluation 事实校验与记录，要求 Evaluation 对应席位当前 Submission、题目、作者、源码哈希和运行时身份。
- 实现基础设施失败事实记录，以及 withdrawn/no-submission 自动事实收敛；所有席位事实齐全后进入 `READY_FOR_ADJUDICATION`，不生成胜负、分数或伪造 `AC/WA`。
- 补充 `ContestFinalFactType`，并确保暂停、评测、基础设施失败和就绪事件只携带安全状态/原因/标识摘要，不携带源码、Prompt、隐藏用例或执行输出。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m pytest -q tests/test_contest_recovery.py
.venv/bin/python -m pytest -q tests/test_contest_actions.py
.venv/bin/python -m pytest -q tests/test_contest_models.py
.venv/bin/python -m compileall -q app tests
```

## 验证结果

- `tests/test_contest_recovery.py`：**4 passed**。
- `tests/test_contest_actions.py`：**9 passed**。
- `tests/test_contest_models.py`：**3 passed**。
- Python `compileall`：通过。
- 测试仅使用 pytest 隔离 SQLite 数据库；未执行持久 PostgreSQL migration、seed、账户/权限写入、部署、真实模型请求或真实 Docker 外部操作。
- pytest 产生既有 `pytest-asyncio` 弃用警告；无测试失败。

## 遇到的问题

- SQLite 读取带时区的 `DateTime` 后可能返回朴素 `datetime`，直接比较会导致恢复和截止时间逻辑不稳定。

## 解决方式

- 在比赛服务内部统一将受控输入转换为 UTC，并将从数据库读取的朴素时间按 UTC 解释；暂停、恢复和 `advance_time` 共用该处理。

## 下一步

- 阶段 E 任务 5 仍待实现 Agent lease、attempt 和 Worker 恢复语义。
- 阶段 E 任务 6/7 的 API 集成、计划索引回填和阶段级验证尚未执行。
- 持久数据库 migration 仍须针对具体环境单独审批后执行。

## 2026-09-21 22:18（Asia/Shanghai）

## 完成内容

- 完成阶段 E 任务 5：新增 `ContestAgentWorker`，实现席位租约互斥、过期租约回收、attempt 序号递增、失败/过期状态记录、成功动作幂等和受权限过滤的 `ContestAgentContext`。
- 完成阶段 E 任务 6：新增认证比赛读取/动作 API、严格错误映射和 `Authorization` CORS 预检；human 与 agent 通过同一动作入口提交，未增加公开比赛创建或 Token 管理端点。
- 完成阶段 E 任务 7：回填阶段 E Spec/实施计划索引和总架构计划中有测试证据的阶段 E 条目；阶段 F/G 保持未实施。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m pytest -q tests/test_contest_recovery.py
.venv/bin/python -m pytest -q tests/integration/test_contest_flow.py
.venv/bin/python -m compileall -q app tests
LLM_PROVIDER=mock ARENA_SANDBOX_INTEGRATION=0 .venv/bin/python -m pytest -q \
  tests/test_contest_models.py \
  tests/test_contest_actions.py \
  tests/test_contest_recovery.py \
  tests/integration/test_contest_flow.py
```

## 验证结果

- 阶段 E 最小充分验证：**25 passed、0 failed、0 skipped**；Python `compileall` 通过。
- 验证覆盖模型/隔离 migration、状态与动作、租约/attempt 恢复、Worker 重启幂等、上下文隐私、认证 API、401/403/409 错误映射、新 Session 读取和 CORS Authorization 预检。
- RED→GREEN 证据：缺少 `app.contests.worker` 时恢复测试收集失败；缺少比赛 Router 时 HTTP 测试返回 404、Authorization 预检返回 400；实现后目标测试全部通过。
- 测试仅使用 pytest 临时 SQLite；未执行持久 PostgreSQL migration、Alembic upgrade、seed、账户/权限写入、部署、真实模型请求或真实 Docker。

## 遇到的问题与限制

- SQLite 的 timezone-aware `DateTime` 读取可能变为朴素时间；Worker 对数据库时间统一按 UTC 解释。
- 当前未验证 PostgreSQL 并发锁/唯一约束下的真实竞态、进程级 Worker 崩溃恢复、真实 Provider/Driver、真实 Judge/Docker 和生产 API 部署链路。
- 阶段 E 不实现阶段 F 的质疑/裁决/计分，也不实现阶段 G 的观战、事件流、公开推演和追问。

## 下一步

- 如需生产落地，先针对具体目标数据库审批并执行 migration 计划，再补充 PostgreSQL 并发和部署环境验证。
- 阶段 F/G 需分别确认业务 Spec 后再实施；不得将本次隔离测试通过解释为生产比赛或裁决验收。
