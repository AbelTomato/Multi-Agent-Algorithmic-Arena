# 2026-09-17

## 完成内容

- 在项目内安装并验证 Go `1.24.0 linux/amd64`；工具链目录 `.tools/` 已忽略，不纳入版本控制。
- 新增 Go 标准库执行控制器模块、冻结的 `GET /health` 与 `POST /execute` 契约、单槽准入、服务端任务 ID、固定 Docker 参数、共享输出预算、取消/关闭路径、OOM 证据分类和标签化清理逻辑。
- 使用 fake Docker 与 Go 测试辅助进程完成单元测试。
- 更新 Sandbox README，记录构建命令、限制、授权边界和当前未覆盖风险。
- 在明确授权后完成真实 Docker 验收：Docker Desktop 29.8.0、cgroup v2、固定 Python 3.11 镜像摘要下，验证正常/非零、JSON stdin/stdout、TLE、stdout/stderr/双流 OLE、HTTP 取消、并发 `409`、可靠 OOM、网络禁用、非 root、只读根目录、64 MiB tmpfs、32 PID 与标签化清理。
- 一次外层测试命令因执行通道 30 秒上限被中止，留下一个已退出的 Arena 自有容器；已先核验双标签和任务 ID 一致，再针对性删除，最终残留检查为空。
- 阶段 2 Judge 核心链路完成：严格版本化用例 schema、UTF-8 输入边界、异步 `SandboxClient(trust_env=False)`、冻结控制器响应校验、可供 API 映射的控制器异常，以及基于当前 `nums/target` 的 Two Sum 语义判定。
- 新增 Judge→Go 控制器真实集成测试，覆盖两题 AC、Valid Parentheses WA、RE、TLE、OLE、可靠 MLE、控制器不可用和单槽 `409`；每组后 Arena 标签容器残留为空。
- 新增请求捕获测试，确认执行器请求只包含候选代码、当前单例输入和 `json-stdio-v1`，不含隐藏期望值、完整用例集、隐藏标记或调用方任务 ID。
- 阶段 3 本地闭环完成：新增默认关闭的 `POST /api/evaluations`、严格 Agent JSON 输出校验、210 秒总期限、控制器/API 故障映射与前端评测摘要；旧 `/api/solutions` Markdown 契约保持不变。
- 新增固定 Agent 的 FastAPI → Judge → Go 控制器真实 API 集成测试，使用 pytest 临时 SQLite，不访问真实 Provider；Two Sum 当前 v1 的 9 个用例均通过，Agent Prompt 与 API 响应不含隐藏期望值。
- 在本次授权范围内启动 loopback Go 控制器并执行真实链路；结束后控制器已停止，8001 未监听且无 Arena 标签容器残留。

## 执行命令

```bash
GO=/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
"$GO" test ./...
"$GO" vet ./...
ARENA_SANDBOX_INTEGRATION=1 "$GO" test ./tests/integration -v -count=1

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/backend
.venv/bin/python -m pytest -q tests/test_judges.py tests/test_judges_stage2.py
ARENA_SANDBOX_INTEGRATION=1 .venv/bin/python -m pytest -q tests/integration/test_judge_sandbox.py

LLM_PROVIDER=mock .venv/bin/python -m pytest -q tests/test_evaluations.py tests/test_agents.py tests/test_solutions.py
ARENA_SANDBOX_INTEGRATION=1 LLM_PROVIDER=mock .venv/bin/python -m pytest -q tests/integration/test_evaluation_flow.py

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/frontend
npm test -- --run src/App.test.tsx src/lib/api.test.ts
npm run build
```

## 验证结果

- `go test ./...`：通过；`cmd/controller` 无测试文件，`internal/executor`、`internal/httpapi`、`internal/protocol` 均通过。
- `go vet ./...`：通过。
- 已验证严格未知字段拒绝、单槽 `409`、关闭取消、UTF-8 字节上限、共享输出恰好/超过上限、退出码 137 非 MLE、可靠 OOM 证据为 MLE、固定参数和标签化清理顺序。
- 真实 Docker 验收分三组运行并通过；超时任务约 5.46–5.47 秒结束（包含容器终止和清理开销的 0.5 秒测试容差），每组后及最终均无 `io.arena.sandbox.owner=true` 标签容器残留。
- 最终 `go test ./... -count=1`、`go vet ./...`、`go build -o /tmp/arena-sandbox-controller ./cmd/controller` 均通过。
- Judge 单元测试最终 `45 passed`；`python -m compileall -q app/judges` 通过。
- 真实 Judge 集成验收分组通过：正确/WA 共 `3 passed`，执行与基础设施错误共 `6 passed`，修复异常保留后的不可用/忙碌回归 `2 passed`；控制器在测试后已停止，8001 未监听且无 Arena 标签容器残留。
- 阶段 3 后端单元与旧接口回归：`30 passed`；前端目标测试：`12 passed`；前端生产构建通过。
- 阶段 3 真实 API 集成：`1 passed`（13.44 秒）；Docker Desktop `29.8.0`、固定 Python 3.11 镜像摘要下，测试后无 `io.arena.sandbox.owner=true` 标签容器残留，8001 未监听。

## 遇到的问题

- Go 官方下载 TLS 连接失败；改由用户从镜像下载并以官方元数据 SHA-256 和文件大小校验后安装。
- 完整集成套件超过执行通道 30 秒上限；改为生命周期、隔离资源、并发/取消/OOM 三组运行，避免中断控制器清理。
- 阶段 2 原型把 `docker_error`/未知结果误归为 `RE`，并以固定期望下标比较 Two Sum；同步客户端还允许调用方任务 ID，且吞并控制器 HTTP 错误，无法供后续 API 映射。
- 在执行通道内直接以后台 shell 启动 `go run` 或临时二进制会被外层 30 秒通道回收；未创建候选容器或产生残留。

## 解决方式

- Go 仅使用标准库和项目内固定版本工具链，未引入第三方依赖。
- Docker Desktop 启动后先复核 daemon、cgroup 和固定镜像，再在获得“创建并删除带固定 Arena 标签的临时容器”授权后执行真实验收；对通道超时残留容器仅在双标签与任务 ID 匹配后进行针对性删除。
- 以失败测试先行：严格 schema 阻止未知字段/空或超量用例；Judge 按输入语义判定 Two Sum；控制器结果仅在可靠 OOM 证据时映射 MLE，Docker/未知执行结果映射 UKE；`409`、连接、超时、5xx 和畸形响应保留结构化异常给 API 层。
- 使用 `setsid nohup` 显式脱离执行通道后启动仅绑定 loopback 的已构建控制器；测试结束后发送 `SIGTERM` 并复核端口与 Arena 标签容器为空。

## 下一步

- 阶段 3 本地退出条件已有固定 Agent 的真实 API 链路证据；不得将其表述为真实 Provider、ECS、强安全隔离或公网安全验收。后续进入阶段 4 的隔离加固与稳定性评估前，仍需独立确认范围与授权。

### 17:31 阶段 4：启动恢复与稳定性加固（单元级）

## 完成内容

- 新增 Go 控制器启动前的 Arena 自有遗留容器恢复：先以 owner 标签筛选，再用完整容器 ID、容器名称、owner 标签和服务端 task-id 标签二次核验；仅对名称与 `arena-task-*` 标签精确一致的任务执行停止和删除。
- Docker 在清理期间返回“资源不存在”时按幂等成功处理；列举、inspect、stop 或 remove 的其他错误会汇总并阻止控制器监听，避免遗留任务未处置时继续接收执行请求。
- 恢复扫描固定使用 `docker ps --all --quiet --no-trunc`，避免短容器 ID 与 `docker inspect .Id` 全 ID 不一致而误拒绝合法恢复。
- 扩展 Sandbox README，记录阶段 4 安全矩阵、rootless Docker/gVisor/独立 VM 的未选型结论和当前风险边界。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
GO=/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go
"$GO" test ./internal/executor -count=1
"$GO" test ./... -count=1
"$GO" vet ./...
"$GO" build -o /tmp/arena-sandbox-controller-stage4 ./cmd/controller
```

## 验证结果

- 新增恢复单元测试通过：已验证自有任务恢复、标签/task-id 不匹配不操作、清理时资源消失、inspect/remove 非不存在错误保留，以及 Docker stderr 中 `No such container` 的幂等识别。
- fake Docker CLI 测试通过：已验证恢复命令序列和 `--no-trunc` 完整容器 ID 参数。
- `go test ./... -count=1`、`go vet ./...` 和控制器构建通过；未调用 Docker daemon，未创建或删除容器。

## 遇到的问题

- `docker ps --quiet` 默认可返回短容器 ID，而 `docker inspect .Id` 返回完整 ID；若直接比较会使真实恢复路径把所有候选判为不匹配。

## 解决方式

- 恢复扫描显式加入 `--no-trunc`，并以 full ID、名称和双标签精确匹配后才清理。

## 下一步

- 已在获得具体临时容器授权后，使用固定镜像和原有安全参数创建一个双标签/名称精确匹配的遗留 Arena 任务；`RecoverStaleTasks` 已在真实 Docker 中停止并删除它，测试后无 owner 标签容器残留。daemon 不可用和 stop/remove/inspect 故障仍仅由隔离单元替身验证，未主动影响共享 daemon。
- 完整真实集成套件再次超过外层 30 秒通道限制并中断；残留的已退出容器经 full ID、owner 标签、task-id 标签和名称一致性核验后已针对性删除，最终残留为空。分组验收中，生命周期的正常/非零/TLE/OLE、JSON stdin/stdout 和隔离资源组均通过；端到端超时返回断言两次实测约 `5.55s` 与 `5.70s`，超过既有 `5.5s` 容差。执行器仍在 5 秒 context 触发终止，但 Docker 清理开销使完整响应超限；v1 Spec 未变，不擅自放宽阈值。
- 已确认保持候选执行 5 秒硬上限不变，并将超时后的同步 inspect、标签校验、stop/remove 清理完成后的端到端返回上限明确为 6 秒；任一真实验收超过 6 秒时不再放宽容差，改为分段计时定位清理长尾。
- 在获得本次三轮临时 Arena 容器授权后，真实超时验收连续 3 次通过：约 `5.53s`、`5.53s`、`5.51s`；每轮均返回 `TLE` 且无 `io.arena.sandbox.owner=true` 标签容器残留。候选执行的 5 秒硬上限未修改。
- 阶段 4 尚未完成；仍不得将 fake Docker、真实单一恢复测试或此前 Docker 证据表述为强安全、共享 daemon 故障恢复、真实 Provider 或 ECS 准入证据。

### 17:55 阶段 4：task ID 随机源失败关闭

- 修复固定 fallback task ID 风险：服务端 128 位加密随机数不可用时，执行器返回 `unknown_error` 且不调用 Docker；不再生成可预测的 `arena-task-fallback` 容器名。该路径由单元测试验证。
- 已执行 `go test ./... -count=1`、`go vet ./...` 和控制器构建，全部通过；本次验证未调用 Docker daemon、未创建或删除容器。

### 18:00 阶段 4：真实控制器进程重启恢复

- 在获得单次具体授权后，新增并执行 opt-in 真实集成测试：第一个 loopback 控制器创建一个受限的活动 Arena 任务后被 `SIGKILL` 终止，替代控制器在启动恢复阶段回收该任务；测试运行 `2.70s` 通过，完成后无 owner 标签容器残留。
- 测试只操作由其创建、且 full ID、owner 标签、task-id 标签和名称均通过核验的容器；失败 defer 以相同核验条件执行 `docker rm --force`。未操作现有不匹配容器，未重启或破坏共享 Docker daemon。
- daemon 短暂不可用与 inspect/stop/remove 故障仍仅有隔离单元测试证据，阶段 4 不得据此宣称共享 daemon 故障恢复或强隔离。

### 18:07 阶段 4：受控隔离矩阵与 Docker 不可达失败关闭

## 完成内容

- 扩展 opt-in 真实 Docker 隔离测试：除非 root、网络阻断、根文件系统只读和 `/tmp` 可写外，额外从 `/proc/self/status` 断言 `CapEff` 为零及 `NoNewPrivs: 1`，直接验证 `--cap-drop ALL` 与 `no-new-privileges` 的容器内可见效果。
- 按串行方式完成真实验收：64 MiB tmpfs 在最多写入 65 MiB 时受限；PID 测试最多 64 次 fork、子进程最多 1 秒且回收；一次 256 MiB 分配取得 Docker `OOMKilled` 证据。各项后均无 Arena owner 标签容器残留。
- 新增 4,096 个服务端 task ID 的有界格式与唯一性测试；这只检查该固定样本，不将其表述为概率性碰撞证明。
- 独立控制器使用不存在的本地 Unix Docker socket 时，在启动恢复扫描阶段失败并且不监听 `127.0.0.1:8001`，取得真实 Docker CLI 不可达的失败关闭证据。
- 只读探测确认当前为 Docker Desktop 29.8.0、默认 `runc`；未发现 rootless Docker 安装工具或 `runsc`，未安装或切换任何运行时。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
ARENA_SANDBOX_INTEGRATION=1 go test ./tests/integration -run '^TestRunnerRealDockerEnforcesNetworkFilesystemAndUserBoundaries$' -count=1 -v
ARENA_SANDBOX_INTEGRATION=1 go test ./tests/integration -run '^TestRunnerRealDockerEnforcesTmpfsLimit$' -count=1 -v
ARENA_SANDBOX_INTEGRATION=1 go test ./tests/integration -run '^TestRunnerRealDockerEnforcesPIDLimit$' -count=1 -v
ARENA_SANDBOX_INTEGRATION=1 go test ./tests/integration -run '^TestRunnerReportsReliableOOMEvidenceWhenDockerProvidesIt$' -count=1 -v
DOCKER_HOST=unix:///tmp/arena-phase4-unavailable.sock /tmp/arena-sandbox-controller-phase4
```

## 验证结果

- 上述四项真实 Docker 测试均通过；每项完成后的 owner 标签容器查询为空。
- Docker CLI 不可达控制器以退出码 `1` 在监听前退出，日志为 `recover stale Arena tasks before startup`；8001 未监听。
- 初次将三个 Docker 写入测试并行运行时，前两个在自身残留断言中观察到另一个测试尚在清理的临时容器而失败；后续残留复核为空，改为串行后全部通过。该问题是共享测试状态时序，不是隔离控制失效。

## 遇到的问题

- Docker Desktop 在当前 Linux 会话通过 `/var/run/docker.sock` 可访问，但 `systemctl` 服务非活动且无法确认安全、可恢复的停止/启动入口。

## 解决方式

- 不物理停止共享 Docker daemon；使用不存在的独立 Unix socket 验证真实 Docker CLI 不可达时的失败关闭，并保留物理 daemon 停止/重启未验证的限制。

## 下一步

- 阶段 4 本地受控退出条件完成；不得将当前结果扩大为共享 daemon 物理故障恢复、rootless/gVisor/VM 隔离、强安全隔离、匿名公网执行安全或 ECS 准入。

### 20:37 阶段 5：ECS 同机评测准入（本地发布资产与只读预检）

## 完成内容

- 用户明确接受 Go 控制器 Docker daemon 高权限与现有 ECS 的 backend、PostgreSQL、Nginx 和 Provider API Key 同机的风险；该决策不改变强隔离、匿名公网执行安全或独立执行主机的限制。
- 控制器新增受控监听地址校验：默认仍为 `127.0.0.1:8001`；仅允许 loopback 或私有 IPv4 的 `:8001`，拒绝公网、通配、主机名和错误端口，且在 Docker 初始化前失败关闭。
- Compose 新增固定 `172.30.0.0/24` internal `arena_sandbox` bridge，仅 backend 连接；PostgreSQL 保持不接入。生产评测仍默认关闭。
- 新增 controller systemd 模板；Nginx 两份模板增加 `/api/evaluations` 的独立 `1r/m` 无 burst 限流和 220 秒读取超时；监控仅在评测启用时检查 controller、私网健康和已退出 Arena 标签容器。
- Workbench 只读确认 ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）运行中，backend/postgres healthy、8000 仅 loopback、5432/8001 未监听、`.env.deploy` 为 `600`、Nginx/monitor/backup active，且无 Arena owner 标签容器。

## 执行命令

```bash
GO=/home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/.tools/go-1.24.0/bin/go
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena/sandbox
"$GO" test ./cmd/controller ./internal/executor ./internal/httpapi ./internal/protocol -count=1
"$GO" vet ./...
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 "$GO" build -trimpath -buildvcs=false -o /tmp/arena-sandbox-controller ./cmd/controller

cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena
.venv/bin/python -m pytest -q tests/test_deployment_config.py
docker compose --env-file .env.deploy.example config --quiet
bash deploy/monitor/tests/test_monitor.sh
```

## 验证结果

- 初次静态测试发现 `arena_sandbox` 被错误加入 PostgreSQL；测试阻止该网络边界错误后已修复为仅 backend 接入。
- ECS 资源读数：2 vCPU、约 1.3 GiB available RAM、1.9 GiB 可用 swap、约 29 GiB 磁盘；Docker 26.1.3、cgroup v1。
- 固定 Python 3.11 候选镜像不在 ECS 本地；本次未拉取镜像、未写入 ECS、未启动控制器、未重建 backend、未调用 Provider、未创建或删除容器。

## 下一步

- 需对拉取固定候选镜像、ECS 上传/配置/服务变更，以及真实 Provider/临时 Arena 容器评测分别取得具体授权；在此之前保持 `EVALUATION_ENABLED=false`。

### 21:39 阶段 5：已授权镜像拉取失败

## 完成内容

- 在用户明确批准拉取固定候选镜像、ECS 发布及一次真实评测验收后，先执行唯一允许的镜像拉取步骤：`python:3.11-slim@sha256:9534e5a8e315485d4061ed659af0fd78a284c015f9b73661b41d6bab25604534`。
- 拉取失败后只读核验镜像仍不存在；未继续上传发布物、创建 systemd 用户/服务、修改 Compose/Nginx/`.env.deploy`、重建 backend、启动控制器、调用 Provider，或创建/删除 Arena 容器。

## 执行命令

```bash
docker pull python:3.11-slim@sha256:9534e5a8e315485d4061ed659af0fd78a284c015f9b73661b41d6bab25604534
```

## 验证结果

- Docker CLI 访问 `https://registry-1.docker.io/v2/` 时发生 `Client.Timeout exceeded while awaiting headers`。
- 后续只读检查确认固定镜像 absent，根分区约 29 GiB 可用；ECS 已存在 `https://uk52zpri.mirror.aliyuncs.com` registry mirror 配置，但本次请求未经该镜像源完成拉取。

## 下一步

- 未经新的具体授权，不修改 Docker daemon 镜像源配置、不重启 Docker、不切换至第三方镜像仓库。需确定经审查的固定 digest 镜像获取来源后再继续发布。

### 21:43 阶段 5：获批 Daocloud 固定镜像拉取

## 完成内容

- 经用户明确批准，改用 `m.daocloud.io/docker.io/library/python:3.11-slim@sha256:9534e5a8e315485d4061ed659af0fd78a284c015f9b73661b41d6bab25604534` 拉取固定候选镜像。
- ECS 返回的 RepoDigest 与锁定 SHA-256 一致，架构为 `linux/amd64`，镜像大小为 `133023775` 字节；根分区仍约 29 GiB 可用。
- 为避免正式 Go 控制器在执行时回退访问已超时的 Docker Hub，已将其固定镜像引用同步为获批 Daocloud repository，Python 版本和 SHA-256 digest 未改变；该变更由 Go 单元测试覆盖。

## 限制

- 本次只拉取并核验镜像；未修改 Docker daemon 配置、未重启 Docker，尚未上传或部署控制器、未重建 backend、未调用 Provider、未创建候选容器。

### 21:47 阶段 5：ECS 后端版本门禁

## 完成内容

- 已将经本地 SHA-256 校验的控制器和部署模板打包上传至 ECS 临时目录，并在未覆盖生产文件前执行归档完整性核验。
- 只读确认当前 ECS backend 缺少 `app.api.evaluations`，对 `/api/evaluations` 的请求返回 `404`；因此当前生产后端未包含阶段 3 评测 API/Judge 代码。

## 下一步

- 在获得“上传当前 backend 源码与 judge_cases、重建 backend 镜像但不执行 migration/seed”的具体授权前，停止发布。临时发布归档保留在 `/tmp/arena-release-20260917/`，尚未解包或覆盖生产路径。

### 22:01 阶段 5：已授权后端发布因依赖源阻塞而回滚

## 完成内容

- 在获批范围内补充 backend 镜像运行时用例目录复制：`Dockerfile` 新增 `COPY judge_cases ./judge_cases`，并以部署静态测试锁定该要求。
- 将不含 `.env`、tests、虚拟环境和 Python 缓存的 backend 运行时归档上传至 ECS 临时目录，SHA-256 校验通过；归档包含评测 API、Judge 源码和两份版本化用例。
- 在覆盖前创建权限为 `700` 的回滚快照，包含原 backend 构建上下文、Compose、`.env.deploy`、Nginx 与 monitor 文件；已安装发布文件后，backend 镜像首次构建因 PyPI 依赖下载阻塞而未完成。
- 受控取消尚未替换线上容器的 BuildKit 构建后，恢复原 backend 构建上下文与所有运行时配置，停止并禁用未启动的 controller；未执行 migration、seed、数据库、volume、镜像删除或 Provider 调用。

## 执行命令

```bash
cd /home/abeltomato/workspace/projects/Multi-Agent-Algorithmic-Arena
.venv/bin/python -m pytest -q tests/test_deployment_config.py

cd backend
LLM_PROVIDER=mock .venv/bin/python -m pytest -q tests/test_evaluations.py tests/test_agents.py tests/test_solutions.py

# ECS：仅 backend 的后台 docker compose build/recreate；构建卡住后取消并恢复快照
docker compose up -d --build --no-deps backend
```

## 验证结果

- 本地部署静态测试：`3 passed`；后端评测与旧接口回归：`30 passed`；`compileall app/judges` 通过。
- ECS 上传的干净 backend 归档 SHA-256 为 `9668f2ceb131bfbc587edcf3990d7bb922f7f93b17a49c809b4a0c217fb3858c`，包含 `app/api/evaluations.py`、`judge_cases/two-sum/v1.json` 和 `judge_cases/valid-parentheses/v1.json`。
- 回滚后 ECS：`/health`、`/api/problems` 均为 `200`；旧 `POST /api/solutions` 对无效 `{}` 返回 `422`（未调用 Provider）；`/api/evaluations` 为 `404`；backend healthy；无 Arena 标签容器；`8001` 未监听；Nginx 配置测试和 reload 成功。

## 遇到的问题

- ECS 的 backend 首次镜像构建在 `pip install -r requirements.txt` 阶段停滞于 PyPI 元数据下载，超过 4 分钟未产生进展；Workbench 单次命令通道另有 30 秒上限，不能直接等待前台构建完成。

## 解决方式

- 使用后台构建日志和退出标记观察，确认卡住的是 BuildKit/Python pip 子进程而非已替换的线上 backend；随后仅终止该未完成构建，并从预先创建的回滚快照恢复全部生产构建上下文和配置。

## 下一步

- 发布保持阻塞。需先获得可靠、经审查的 Python 依赖获取/预构建镜像方案，再重新执行仅 backend 构建；在 backend 评测 API 能以 `EVALUATION_ENABLED=false` 返回 `503` 前，不启动 controller、不发布评测 Nginx/monitor 配置，也不进行真实 Provider 或候选容器验收。

### 23:53 阶段 5：ECS Python 依赖源完整下载预检

## 完成内容

- 阅读 ECS 历史构建日志确认：2026-09-10 backend 构建曾成功，但默认 PyPI 的 `pip install` 阶段耗时约 936 秒；本次默认源构建又在元数据下载阶段停滞，不能作为当前稳定构建来源。
- 对官方 PyPI、阿里云 HTTPS 和清华 HTTPS 镜像的项目页进行只读 Range/TLS 检查；阿里云与清华均可读取 11 个顶层依赖项目页及 FastAPI 示例 wheel 首字节。ECS 历史 `mirrors.cloud.aliyuncs.com` 配置因 HTTP/`trusted-host` 或 HTTPS 证书域名不匹配被排除。
- 经具体批准后，使用 ECS 已存在的 `python:3.12-slim` 创建自动删除的临时容器，以阿里云 HTTPS PyPI 镜像完整下载当前 backend 的直接与传递依赖；未安装依赖、未重建 backend。
- 宿主机 Python `3.6.8` / pip `9.0.3` 的首次预检因不兼容 `fastapi==0.115.6` 失败，临时目录未写入包；随后在与 production backend 一致的 Python 3.12 环境中完成有效预检。

## 执行命令

```bash
docker run --rm \
  --label io.arena.preflight=python-dependency-download-20260917 \
  --mount type=bind,src=/opt/multi-agent-arena/app/backend/requirements.txt,dst=/requirements.txt,readonly \
  --mount type=bind,src=/tmp/arena-pip-preflight-20260917,dst=/downloads \
  python:3.12-slim \
  python -m pip download \
    --disable-pip-version-check \
    --dest /downloads \
    --index-url https://mirrors.aliyun.com/pypi/simple/ \
    -r /requirements.txt
```

## 验证结果

- 阿里云 HTTPS PyPI 源在 Python 3.12/Linux amd64 环境中成功解析并下载 34 个直接与传递依赖 wheel，下载集约 18 MiB；包含 Python 3.12/Linux amd64 专用 wheel 与通用 wheel。
- `/tmp/arena-pip-preflight-20260917` 权限为 `700`；`/tmp/arena-pip-preflight-20260917.sha256` 含 34 条 SHA-256 记录。
- 临时预检容器因 `--rm` 自动删除；无 Arena 标签候选容器。backend、PostgreSQL 均 healthy，controller inactive，`/health` 返回 `200`。

## 限制与下一步

- 下载集和 SHA-256 清单保留在 ECS `/tmp` 供审计；未获删除许可，未清理它们。
- 当前仅证明阿里云 HTTPS 源可在目标 Python/平台下载当前依赖树，尚未基于该下载集生成并审查 production `--require-hashes` lock/constraints 文件，也未重建 backend。
- 后续若生成哈希锁定文件、将依赖源写入 Dockerfile/构建参数，或重新构建 backend，均需在明确范围内继续执行；保持 `EVALUATION_ENABLED=false`。