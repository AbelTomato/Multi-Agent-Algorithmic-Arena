# ECS 同机评测准入计划

> **所属日期：** 2026-09-17（Asia/Shanghai）。
> **计划时间：** 20:37（Asia/Shanghai）。
> **执行方式：** 后续使用 `executing-plans` 按任务执行；远程写操作、镜像拉取、Provider 调用、Arena 容器创建/删除和数据库操作均需分别获得针对具体范围的授权。

## 目标与已接受风险

在 Alibaba Cloud ECS `i-wz95igsjwjrfzpckjip5`（`cn-shenzhen`）受限启用现有 `POST /api/evaluations`。用户明确接受 Go 控制器的 Docker daemon 高权限与 backend、PostgreSQL、Nginx 和 Provider API Key 同机；该选择不构成独立执行主机、强隔离、匿名公网代码执行安全或高可用。

控制器作为 systemd 宿主机进程运行，backend 通过固定 `arena_sandbox` Docker internal bridge 的网关 `172.30.0.1:8001` 调用它。PostgreSQL 不连接该网络；不公开 TCP/8001、8000 或 5432。`EVALUATION_ENABLED` 默认保持 `false`。

## 已完成的本地准备

- [x] 控制器仅允许 `ARENA_SANDBOX_LISTEN_ADDR` 为 loopback 或私有 IPv4，且严格使用端口 `8001`；默认地址仍为 `127.0.0.1:8001`。
- [x] Compose 增加 `172.30.0.0/24` 的 internal `arena_sandbox` 网络，仅 backend 加入。
- [x] 部署示例新增评测开关、210 秒总期限和私有控制器 URL，默认关闭。
- [x] Nginx 的两份模板对 `/api/evaluations` 使用独立 `1r/m`、无 burst 的限流及 `220s` 读取超时。
- [x] 新增 controller systemd 模板；扩展监控，在启用评测时检查 service、私网健康和已退出 Arena 标签容器。

## 只读预检结果（2026-09-17）

- [x] 目标实例为 Running，Alibaba Cloud Linux 3、2 vCPU；约 1.3 GiB 可用内存、1.9 GiB 可用 swap、约 29 GiB 可用磁盘。
- [x] Docker `26.1.3`、cgroup v1；backend/postgres 均 healthy，8000 仅绑定 loopback，5432 与 8001 未监听，`.env.deploy` 为 `600`，无 Arena owner 标签容器。
- [x] Nginx、监控 timer 与备份 timer active；现有配置尚未有 controller、评测 Nginx 规则或评测监控。
- [x] ECS 已通过获批来源 `m.daocloud.io/docker.io/library/python:3.11-slim@sha256:9534e5a8e315485d4061ed659af0fd78a284c015f9b73661b41d6bab25604534` 拉取候选镜像；`RepoDigest` 与锁定 SHA-256 一致，架构为 `linux/amd64`，镜像约 133 MB。

**2026-09-17 镜像获取记录：** 首次直接访问 Docker Hub 超时，未造成后续发布变更。经明确批准后，改用 Daocloud 同一精确 digest 并成功拉取；`RepoDigest` 为 `m.daocloud.io/docker.io/library/python@sha256:9534e5a8e315485d4061ed659af0fd78a284c015f9b73661b41d6bab25604534`，`linux/amd64`，大小约 133 MB，根分区仍约 29 GiB 可用。Go 正式控制器固定引用已同步至这个获批来源；未修改 Docker daemon 配置或重启 Docker。

**2026-09-17 发布门禁：** 已上传控制器/部署模板归档到 `/tmp/arena-release-20260917/` 并核验归档 SHA-256；尚未解包或覆盖生产路径。随后只读确认当前 ECS backend 容器无法导入 `app.api.evaluations`，`POST /api/evaluations` 返回 `404`。当前 ECS 后端未包含阶段 3 评测 API/Judge 代码，不能仅发布控制器和配置。继续发布需单独批准上传当前 backend 源码与 judge_cases、重新构建 backend 镜像并验证旧 MVP；不需要也不得执行 migration/seed。

## 后续执行顺序与审批

1. 取得“从 Docker registry 拉取上述固定 digest 镜像”的具体许可；只允许该镜像下载，不构建或删除镜像。
2. 取得 ECS 发布许可：上传经 SHA-256 校验的 Linux/amd64 控制器二进制，创建专用 systemd 用户/服务，备份并更新 Compose、Nginx、监控与 `.env.deploy`，重建 backend、reload Nginx。禁止 migration、seed、数据库/volume 修改、镜像删除、安全组/防火墙变更。
3. 保持 `EVALUATION_ENABLED=false`，验证 controller 私网健康、`/health`、`/api/problems`、评测禁用 `503` 以及 Nginx 对无效评测请求的 `422/429`；该阶段不调用 Provider、不创建候选容器。
4. 单独取得真实验收许可后，才设置 `EVALUATION_ENABLED=true` 并对 `problem_id=1`（Two Sum v1，共 9 个用例）发送一次请求。该操作将调用一次 Provider，并最多顺序创建/删除 9 个 Arena 标签容器；`problem_id=2` 为 Valid Parentheses v1，共 12 个用例，必须在批准中单独按 12 次上限声明。验收后确认 controller active、总时限不超过 220 秒、响应仅含摘要字段、无 Arena owner 标签残留。

## 回滚与关闭

发布前备份 Compose、`.env.deploy`、Nginx、monitor script/unit 和 controller unit。任一健康、网络或验收异常时：恢复这些文件，设置 `EVALUATION_ENABLED=false`，重建 backend，停止 controller，`nginx -t` 成功后 reload，并验证 `/health` 与评测禁用 `503`。不得执行数据库 downgrade/restore、volume 删除、`docker system prune` 或无目标容器删除。

## 不可宣称

本计划完成后仍不得宣称强隔离、rootless/gVisor/独立 VM 防护、Docker daemon 物理故障恢复、匿名公网执行安全、长期 Provider 稳定性、ECS 容量充分或高可用。